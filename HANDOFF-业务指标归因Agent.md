# 交接文档 · 业务指标归因 Agent

> **写给下一个窗口 / 下一个协作者**  
> 最近更新：2026-08-06（本会话收束）  
> 仓库：`growth-engine`（营销增长工作台 Demo，React + Vite）  
> 本地预览：`npm run dev` → 通常 `http://localhost:5176/`  
> 主文档：`业务指标归因Agent-工作方法论-v1.0.md`（修订已到 **v1.9**）  
> 指标判定源：Obsidian `中企动力AI产品/AI营销转化组/指标判定逻辑.md`（异常/提升/持平三表）  
> 产品方案：`营销增长工作台-业务指标归因Agent-产品方案-v0.1.md`（部分口径落后，以方法论 + 本交接为准）

---

## 0. 本会话做到哪了（先读这段）

### 0.1 一句话

第 5 步已改成**只写任务说明、不指定调谁**；第 1 步体检阈值换成《指标判定逻辑》三态表，并补了**平均访问时长、核心转化页到达率**的定段/下钻。内容运营侧 skill 已摸底，但**不写进第 5 步调度字段**——任务说明用中文条目对齐入参即可。

### 0.2 本会话已完成

| 项 | 结果 |
| --- | --- |
| §8 应对方案大改（v1.6） | **任务说明**口径：不写调度 skill / 措施类型 / 给谁看 / 备注 / 建议边界；8 种业务任务类型 + 中文入参清单 + A～F/健康度/站外写全示例（去 AI 腔） |
| §9 执行同步（v1.6） | 三档：**内容类确认后落地** / **健康度直接修** / **站外或人工仅展示**；方案默认客户都看得到 |
| opsclaw skill 摸底 | 已访问内网 skills 页并拉正文到 `_tmp_skills/`；确认一期内容能力 6 条（见 §3），但归因侧第 5 步**不 activate**，只出任务说明给内容运营认领 |
| 指标判定三态表（v1.7） | 覆盖 §4.2.1 原默认阈值表；环比对比**过去 4 周同期平均**；含异常/提升/持平 |
| 删核心转化页流失率（v1.8） | 三态表重新编号；只保留**核心转化页到达率** |
| 新指标接入后续流程（v1.9） | **平均访问时长 → 落地页**；**核心转化页到达率 → 站内浏览**；排查树/直达定段/§6.1 问法/§6.3 看数/原因库已补 |
| 文风 | 任务说明与案例用人话；字段名用中文（页面地址、修改要求…），不对客户堆英文参数名 |

### 0.3 本会话未完成 / 卡点

1. **产品方案 / Demo mock / `skills/` 草案**仍可能残留「跌幅 TOP5」「自动执行」「调度 5 子 Agent」「L4」等旧表述，未全量扫齐。  
2. **Obsidian《指标判定逻辑》**若仍含「核心转化页流失率」，需与方法论 v1.8 对齐（方法论已删）。  
3. **第 5 步 ↔ 内容运营认领**的产品交互未定：客户确认后，任务说明如何进「内容运营」大功能（一期可能是对话式交给运营助手，二期再独立子 Agent / A2A）——方法论 §9 只写了边界，没写产品交互稿。  
4. Demo 措施卡仍可能是旧「调度子 Agent」字段，未随 §8 改成任务说明展示。

---

## 1. 项目是做什么的（背景速览）

**业务指标归因 Agent（线 B）**：定时体检业务指标 → 发现变化（异常/提升/持平）→ 5 段漏斗归因 → 出**任务说明**/夸赞/建议 → 异常类按边界落地（内容确认 / 健康度直修 / 站外仅展示）→ 复盘沉淀。

不要和线 A「网站健康度体检」搞混：线 A 看技术/SEO 检测项；线 B 看留资/UV/跳出/访问时长/转化页到达/渠道/行为/意图等。

七步：1 体检 → 2 定段 → 3 下钻 → 4 假设验证 → 5 应对方案 → 6 执行 → 7 复盘。

已拍板要点（跨会话累计 + 本会话）：

- 扫描入口 = **第一层扫描指标**（见 §4 判定表；含访问时长、核心转化页到达率）；漏斗是定段地图，不是扫描入口  
- 第 2 步只定段、不调第二层；按段看数在第 3 步  
- **第 5 步只写「要做什么」**，不指定调哪个 Agent/skill；任务说明尽量按现网能力入参写细（中文条目）  
- 第 6 步：内容类（发/改内容等）**客户确认后**交给内容运营；**健康度可直接修**；投放/外链/表单/客服等**只展示**  
- CTA = 页内固定区块；智能客服 = 角落浮窗  
- 页面排行：**高流量 TOP + 高流量跳出最差**；不要用「转化率最差 TOP5」  

---

## 2. 方法论当前水位（2026-08-06 · v1.9）

| 章节 | 状态 | 备注 |
| --- | --- | --- |
| §1 七步总览 | ✅ 已随 v1.9 改扫描入口与定段/下钻示意 | |
| §4 第 1 步体检 | ✅ 三态判定表已换；模型兜底仍在 | 源：Obsidian《指标判定逻辑》 |
| §5–§6 定段/下钻 | ✅ 新指标直达定段 + 问法 + 看数已接 | 时长→落地页；到达率→站内浏览 |
| §7 假设验证 | ✅ 大体可用；原因库已轻量补新指标 | |
| **§8 第 5 步应对方案** | ✅ **本会话主战场已改完** | 任务说明口径；示例 A～F + 健康度 + 站外 |
| §9 执行 | ✅ 已与 §8 对齐三档边界 | 交互/认领细节可再写 |
| §10 复盘 | ⚠️ 可能仍有旧词 | 建议全文搜「自动执行」「调度子 Agent」 |
| 产品方案 / Demo | ⚠️ 落后 | 见 §0.3 |

---

## 3. 内容能力 / opsclaw skill（摸底结论 · 供任务说明对齐，不进 §8 调度字段）

内网：`http://10.24.60.203:8000/opsclaw/static/skills.html`  
API：`GET /opsclaw/api/skills/{name}`，Header `X-User-Id: admin`（用 curl 落盘防编码乱码）。

架构认知（用户已确认）：这些是**同一运营助手 Agent 下的多条 skill**，不是 6 个独立 Agent；一期归因是出任务说明给人/内容运营看，不是在 §5 里 A2A activate。

### 3.1 业务叫法 ↔ skill（6 条内容 + 健康度另算）

| 业务任务类型（§8.2） | skill / 链路 | 关键入参（任务说明用中文写） |
| --- | --- | --- |
| 生成/重做营销落地页 | `smart_landing_page` | 页面类型、公司、行业、产品、卖点、买家、联系方式… |
| 修改已有产品详情 | **仅** `product_edit_flow`（内部已调 `publish_product`，**不要再挂** `product_publish` skill） | 产品详情地址、修改方式 A/B/C、修改要求、可核实事实 |
| 生成产品详情页 | `image_content_generation` → 其内再 activate `product_publish` | 产品核心词/描述、语言、必须覆盖、发布建议 |
| 修改已有页面文案 | `content_product_edit_flow` | 页面地址 + 修改要求（一般保留标题/摘要） |
| 生成并发布单篇内容 | `content_generation_and_publishing` | 主题、关键词、角度；单篇，默认不挂计划 |
| 建立持续内容发布计划 | `ai_publish_plan` | 主题、5～8 关键词、发布频率（天） |
| 健康度修复 | 线 A / 健康度能力 | 问题类型、影响范围、建议动作 → **直接修** |

本地缓存：`_tmp_skills/*.json`、`*.body.md`（可删可留）。

### 3.2 第 5 步与 skill 的关系（重要）

- §8 **不写** `activate_skill: xxx`  
- 任务说明按上表把原料写全（中文），方便内容运营认领后少追问  
- 「发布内容（单篇）」≠「AI 发布计划（持续）」——用户已拍板

---

## 4. 新指标速记

| 指标 | 体检 | 定段 | 下钻重点 |
| --- | --- | --- | --- |
| 平均访问时长 | 异常/提升/持平均有 | **落地页** | 全站还是某渠×某页短？和跳出是否同向？ |
| 核心转化页到达率 | 异常/提升/持平均有 | **站内浏览** | 哪些入口到不了联系/表单？缺入口还是不点？ |
| ~~核心转化页流失率~~ | **已删** | — | 不要加回去，除非产品重新拍板 |

环比默认对比：**过去 4 周同期平均**（不是只比上一周）。

---

## 5. Demo 原型水位

仍可演示：任务中心 → 归因 Tab → 左清单右对话 → 确认执行 → 复盘。

关键文件：`src/types.ts`、`src/data/attributionMock.ts`、`AttributionReportView.tsx` 等。

**建议下一轮**：措施卡字段改成「任务类型 / 目标对象 / 任务说明 / 依据卡片」；去掉「调度子 Agent」；健康度任务标可直接修。

---

## 6. 下一步该干什么（按优先级）

### P0

1. **扫旧口径**：方法论/产品方案/Demo/skills 搜「调度子 Agent」「5 个子 Agent」「跌幅 TOP5」「自动执行」「L4」「只出方案（技术）」等，按 v1.6～v1.9 改掉或标注废弃。  
2. **对齐 Obsidian《指标判定逻辑》**：删流失率（若还在）；与方法论判定表保持一份真相。  
3. **（可选）Demo**：mock 一条「任务说明」案例 + 健康度直接修案例。

### P1

- 写清「客户确认后 → 内容运营认领」产品路径（一期对话运营助手 vs 二期独立子 Agent）  
- §10 复盘与串讲示例再扫一遍旧词  

### P2

- 编排层 `skills/`（growth-orchestrator 等）与方法论对齐  
- 核心转化页「联系我们页」等配置口径（到达率分母：会话还是 UV）落数据字典  

---

## 7. 新窗口开场建议 Prompt

```text
我在做「营销增长工作台 · 业务指标归因 Agent」。
请先读 HANDOFF-业务指标归因Agent.md（尤其 §0、§2、§3、§6），
再读 业务指标归因Agent-工作方法论-v1.0.md（修订已到 v1.9）。

关键口径：
- 第 5 步只出「任务说明」，不指定调哪个 Agent/skill；
- 第 6 步：内容确认后落地 / 健康度直接修 / 站外仅展示；
- 新指标：平均访问时长→落地页；核心转化页到达率→站内浏览；流失率已删。

请从 P0 继续：扫旧口径，或按需要改 Demo 措施卡。
```

---

## 8. 文件速查

```text
growth-engine/
├── HANDOFF-业务指标归因Agent.md              ← 本文（2026-08-06）
├── 业务指标归因Agent-工作方法论-v1.0.md       ← 主战场（v1.9）
├── 营销增长工作台-业务指标归因Agent-产品方案-v0.1.md  ← 部分落后
├── _tmp_skills/                              ← opsclaw skill 缓存（可选删）
├── skills/                                   ← 归因侧 skill 草案
└── src/                                      ← Demo 原型
```

Obsidian（本机，非仓库）：  
`.../AI营销转化组/指标判定逻辑.md`

---

## 9. 历史坑（仍有效）

1. 不要在数据看板塞归因入口；任务中心 → 诊断报告 Tab → 整页详情。  
2. 归因不做 NLP 意图识别；用 L3 意图数据，复盘回流。  
3. 「转化率最差 TOP5」对 B2B 误报严重——不要改回去。  
4. 写方法论用人话、完整因果句；任务说明字段用**中文**，少 AI 腔。  
5. **详情修改不要双重发布**：只走 `product_edit_flow`（自带回写）；新详情才是生成 → `product_publish`。  
6. 第 5 步不要倒回去写「调度哪个 skill」——那是内容运营认领后的事。
