# 交接文档 · 业务指标归因 Agent

> **写给下一个窗口 / 下一个协作者**
> 日期：2026-07-30
> 仓库：`growth-engine`（营销增长工作台 Demo，React + Vite）
> 本地预览：`npm run dev` → 通常 `http://localhost:5176/`

---

## 1. 这个项目是做什么的

### 1.1 大背景

「数字门户 V2.0 / 营销增长工作台」是给 B 端客户用的增长运营平台：看站数据、体检网站健康度、管线索、运营内容。整体架构是老板定义的 6 层（L1 业务目标 → L6 技术底座）。

### 1.2 你负责的部分

**业务指标归因 Agent**（产品方案里也叫「线 B」）

- **架构归属**：L4 产品应用层 · **全链路数据分析** 模块
- **能力输入**：主要吃 L3「行为数据漏斗分析」；意图相关能力对接 L3「搜索意图精准匹配」（使用方 + 反哺方，不重复建设意图理解）
- **一句话价值**：定时体检业务指标 → 发现变化（异常/提升/持平）→ 6 段漏斗归因下钻 → 出方案/夸赞/建议 → 异常类调度现有 L4 模块执行 → 复盘沉淀

**不要和「线 A · 网站健康度体检」搞混**：

| | 线 A 健康度 | 线 B 归因（本方案） |
| --- | --- | --- |
| 看什么 | 六维技术/SEO/内容等检测项 | 留资、UV、跳出、渠道、漏斗行为 |
| 触发 | 检测计划 / 手动检测 | **仅定时**（站点设置配置周期，≥7 天） |
| 产出 | 问题清单 + 修复任务 | 三类变化报告 + 措施/夸赞/建议 + 复盘 |

两条线共用「智能体任务」中心做任务概览，但分析链路独立。

---

## 2. 核心设计（新窗口必须先读懂）

### 2.1 6 段转化漏斗（分析骨架）

```text
渠道到达 → 落地页 → 站内浏览 → 转化入口触发 → 转化交互 → 成功留资
```

每段有「第一层信号 / 第二层下钻」两套数据（已在方案 §3.3 与方法论 §1.1 对齐）。

### 2.2 三类变化

- **异常（down）**：归因 → 措施（依据卡片）→ 执行 → 复盘
- **提升（up）**：归因 → 夸赞（不执行）
- **持平（flat）**：归因 → 说明 + 提升建议（只出方案不执行）

### 2.3 Agent 七步工作流（方法论）

1. 指标体检 → 2. 漏斗段定位 → 3. 归因下钻 → 4. 假设验证 → 5. 应对方案 → 6. 执行 → 7. 复盘沉淀

### 2.4 三层数据视野

- **第一层**：6 类必看（北极星/流量/承接/渠道/健康度/页面排行）——每次体检必拉
- **第二层**：按需调取——渠道/页面明细、4 类行为埋点、漏斗留存率、**用户意图数据**
- **第三层**：图表/展示——不进 Agent

### 2.5 关键产品决策（已拍板）

- 触发：**只定时**，周期在站点设置配置（7/14/30 天）
- **去掉客户分层（S/A/B/C）**：所有客户同一套能力
- 去掉「客户分层与行业基准」作为第二层数据类
- UI：诊断报告 Tab（健康度 | 业务指标归因）；任务中心统一入口；归因详情是**整页左清单+右 Agent 对话**，不是抽屉
- 措施必须带**依据卡片三段式**（现状 / 对比 / 动作）
- 执行边界：自动 / 确认后 / 只出方案；提升与持平不进执行

---

## 3. 文档做到什么程度

| 文档 | 路径 | 当前版本 | 状态 |
| --- | --- | --- | --- |
| **产品方案** | `营销增长工作台-业务指标归因Agent-产品方案-v0.1.md`（根目录；`资料/` 下有旧副本，以根目录为准） | **v1.4** | 模块 1-6 + 9 例案例 + 附录 A/B 已定稿口径 |
| **工作方法论** | `业务指标归因Agent-工作方法论-v1.0.md` | **v1.3**（内容已更新；文首版本号仍写 v1.0，可顺手改） | 七步流程 + 数据食谱 + 护栏 + 汇报一页纸 |
| 本交接文档 | `HANDOFF-业务指标归因Agent.md` | 本文 | — |

### 产品方案要点（v1.4）

- 模块 1：指标体系（含 4.3.4 用户意图数据）
- 模块 2：指标变化发现（三类变化，定时触发）
- 模块 3：归因分析（6 段定位 + 假设验证 + 三套原因库；意图细分三种错配/缺口）
- 模块 4：应对产出（措施/夸赞/建议 + 依据卡片）
- 模块 5：执行（三档边界，调度 L4 五大模块）
- 模块 6：复盘（T+3/7/30 + 沉淀知识库）
- 案例集：**9 例**（含案例 9「意图内容缺口 → 智能营销页生成 Agent」）

### 工作方法论要点（v1.3）

- §1.1 漏斗表已拆「第一层信号 / 第二层下钻」，与数据食谱同名同口径
- §3 数据食谱第二层四类：明细 / 埋点 / 漏斗留存 / 意图
- §14 有可直接念的汇报一页纸

---

## 4. 原型代码做到什么程度

技术栈：React + Vite + TypeScript，状态在 `WorkbenchContext`。

### 已落地（可演示闭环）

| 能力 | 文件 |
| --- | --- |
| 类型 | `src/types.ts`（ChangeType、AttributionChange、AttributionReport、AgentTaskRow…） |
| Mock | `src/data/attributionMock.ts`（本期报告约 7 条变化 + 历史报告 + 归因任务） |
| 诊断报告 Tab | `src/views/ReportView.tsx`（网站健康度 \| 业务指标归因） |
| 归因详情页 | `src/views/AttributionReportView.tsx`（左清单 + 右 Agent 气泡：分析/结论/措施/夸赞/持平/复盘） |
| 任务中心 | `src/views/HistoryTasksViews.tsx`（归因报告区 + 健康度修复区，分色模块标签） |
| 状态与动作 | `src/context/WorkbenchContext.tsx`（打开报告、选变化、确认/忽略措施、模拟执行与复盘） |
| 站点设置 | `src/views/WeeklySettingsViews.tsx`（归因开关 + 周期 7/14/30） |
| Agent 目录 | `src/data/mock.ts`（业务指标归因 Agent） |
| 样式 | `src/styles.css`（Tab、布局、气泡、依据卡片、模块标签） |

### 已验证的演示动线

1. 侧栏「智能体任务」→ 归因分析报告 →「查看报告」
2. 落到诊断报告「业务指标归因」Tab
3. 左选变化 → 右看 Agent 对话式归因
4. 异常类点「确认执行」→ 状态流转 → 约 8 秒后复盘「起效」
5. 提升类无执行按钮（夸赞）；持平类只有建议（只出方案）

### 原型未对齐方案的缺口（重要）

| 缺口 | 说明 |
| --- | --- |
| ~~Mock 缺案例 4~~ | ✅ 已补：SEO 收录减少 · T+30 未改善 · 失败沉淀（静态展示弃用+重开归因） |
| ~~Mock 缺案例 9~~ | ✅ 已补：意图内容缺口 · 智能营销页生成，含意图数据气泡 + 复盘回流 L3 |
| ~~意图数据未进 mock 结构~~ | ✅ 已补：`IntentEvidence`（站内搜索/来路关键词/客服对话意图）+ 案例 9 渲染意图气泡 |
| ~~复盘时序不符~~ | ✅ 已改：措施带 `reviewPeriod`（T+3/7/14/30 分档）；执行成功→「待复盘 T+N」→ 到点才 done，任务中心显示待复盘徽章 |
| ~~执行产出不可见~~ | ✅ 已补：`MeasureDeliverable`（page/rewrite/log/ab_test），措施卡片执行后展示「产出已就绪 + 链接 + 预览说明」 |
| 文首版本号 | 方法论文首仍写配套方案 v1.1，实际应对齐方案 v1.4 |
| 历史报告切换 | `openAttributionReport` 找历史报告但不 `setAttributionReport(found)`，点历史任务详情仍读本期报告（待修） |
| 显著性 | 变化判定是阈值规则 + 对照比数，未用 t 检验等推断统计（如需要另开一期产品决策） |

---

## 5. 关键历史（避免重复踩坑）

1. **第一版原型被否**：在数据看板塞「指标变化报告」、用抽屉看归因——老板/用户反馈不行；已回退远端后重做。
2. **现行 UI 方案**：任务中心全局入口 → 诊断报告 Tab 承载归因看板 → **整页详情**（不要「跳转再开抽屉」）。
3. **产品方案经历大重构**：对齐数字门户架构 → 三类变化 → 意图数据 → 去掉客户分层 → 漏斗表拆两层数据。
4. **意图能力边界**：归因 Agent 不自己做 NLP 意图识别；调 L3 意图数据做归因，复盘结果回流意图库。

---

## 6. 建议下一步（优先级）

1. **补原型 mock**：案例 4（失败复盘）+ 案例 9（意图缺口 → 智能营销页生成 Agent）
2. **方法论文首版本号**与配套方案版本对齐到 v1.4 / 方法论 v1.3
3. **汇报**：直接用方法论 §14「汇报一页纸」；深挖用七步 + 案例 1/9
4. 若继续改原型：保持现有导航闭环，不要再往数据看板塞归因入口（已明确否决）

---

## 7. 新窗口开场建议 Prompt

可直接粘贴：

```text
我在做「营销增长工作台 · 业务指标归因 Agent」。
请先读交接文档 HANDOFF-业务指标归因Agent.md，
再读产品方案（根目录，v1.4）和工作方法论。
当前原型已跑通：任务中心 → 归因 Tab → 左清单右 Agent 详情 → 确认执行 → 复盘。
接下来请按交接文档「建议下一步」继续。
```

---

## 8. 文件速查

```text
growth-engine/
├── HANDOFF-业务指标归因Agent.md          ← 本文
├── 营销增长工作台-业务指标归因Agent-产品方案-v0.1.md   ← 产品方案 v1.4（以根目录为准）
├── 业务指标归因Agent-工作方法论-v1.0.md               ← 方法论 v1.3
├── 资料/                                             ← 有旧副本，勿当最新源
└── src/
    ├── types.ts
    ├── data/attributionMock.ts
    ├── context/WorkbenchContext.tsx
    └── views/
        ├── ReportView.tsx
        ├── AttributionReportView.tsx
        ├── HistoryTasksViews.tsx
        └── WeeklySettingsViews.tsx
```
