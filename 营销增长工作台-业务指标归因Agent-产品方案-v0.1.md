# 营销增长工作台 · 业务指标归因 Agent · 产品方案

> 文档目的：定义"营销增长工作台"中"业务指标归因 Agent"的产品方案，作为研发、数据、设计、评审的统一依据。
> 面向对象：研发、数据/神策、设计、产品、业务负责人
> 整理日期：2026-07-30
> 版本：v1.1（重构版·三类变化扩展，对齐数字门户 V2.0 产品架构）

---

## 0. 文档结构与进度

本方案按"定位 → 谁用 → 整体架构 → 六个模块 → 案例 → 落地"组织，对应归因 Agent 从看数到复盘的完整闭环。


| 章    | 主题                                     | 状态  |
| ---- | -------------------------------------- | --- |
| 1    | 背景与定位（在整体架构中的位置）                       | ✅   |
| 2    | 用户与场景（谁用、定时触发、体验闭环·三类变化）               | ✅   |
| 3    | 产品架构与数据流（六模块 + 调度关系 + 6 段漏斗骨架）         | ✅   |
| 4    | 模块 1 · 指标体系（看什么）                       | ✅   |
| 5    | 模块 2 · 指标变化发现（什么算变化·三类、定时触发周期可配）       | ✅   |
| 6    | 模块 3 · 归因分析（为什么变化·三类都归因、怎么定位原因）        | ✅   |
| 7    | 模块 4 · 应对产出（按类型：异常→措施 / 提升→夸赞 / 持平→建议） | ✅   |
| 8    | 模块 5 · 执行（只接异常类、自动/确认/只出方案边界）          | ✅   |
| 9    | 模块 6 · 复盘（效果怎么样、怎么沉淀回知识库）              | ✅   |
| 10   | 端到端案例集（8 例·三类变化闭环演示）                   | ✅   |
| 11   | 落地计划与分期                                | ✅   |
| 附录 A | 落地架构（方案 A · 单 skill 渐进版）               | ✅   |
| 附录 B | 目标架构（方案 B · 编排层 + 子 skill，待启用）         | 预告  |


---



## 1. 背景与定位



### 1.1 业务背景

中企动力核心产品为数字门户（B2B 企业网站）。增长闭环为：

```text
引流侧（SEO / GEO / AI 生成内容发社媒）
  → 流量进入网站（数字门户）
    → 营销页 / 产品详情页 / 列表页 / 询盘
      → 转化（留资 / 询盘 / 成交）
```

"营销增长工作台"是面向 B 端客户的运营平台，客户在其中查看站点数据、诊断网站健康度、处理问题、运营内容、管理线索。工作台已有"数据看板"展示留资、UV、渠道转化等业务指标。

### 1.2 在整体架构中的位置（数字门户 V2.0）

本方案归属数字门户 V2.0 产品架构的 **L4 产品应用层 ·「全链路数据分析」模块**。它的产出定位是 **"输出 · 优化闭环"**——不是输出一份归因报告，而是驱动优化闭环。

```text
L1 业务目标层        转化率 0.5% → 3%+
L2 转化问题解决层    ← 本方案对应「数据闭环」问题（不知道哪里出问题，无法持续优化）
L3 AI 核心能力层     ← 支撑本方案的是「行为数据漏斗分析」能力（全链路埋点 + 自动漏斗诊断 + AI 瓶颈识别）
L4 产品应用层        ← 本方案在此：全链路数据分析（内容→页面→线索归因 + 漏斗诊断 + 线索质量评分 + 销售转化贡献追踪 + 优化策略自动推荐）
L5 数据与知识基座层  ← 喂给本方案的是「行为数据资产 + A/B 测试与效果数据」
L6 技术基础设施层    全链路埋点系统
```

**关键定位**：归因 Agent 不是孤立工具，而是 L4 的"大脑"。它接收 L3 的漏斗分析能力 + L5 的行为数据资产，产出优化策略，**调度 L4 其他 4 个产品模块**（AI 内容引擎、AI 智能客服 PRO、智能表单系统、转化路径设计器）去执行，形成闭环。这与老板会议明确的"运营助手 = 网站核心大脑"定位一致。

### 1.3 与线 A 健康度体检的区别（两条分析线，不要混）

工作台存在**两条并行的分析线**，本方案只针对线 B。


|     | 线 A · 网站健康度体检                                   | 线 B · 业务指标归因分析（本方案）                         |
| --- | ----------------------------------------------- | ------------------------------------------- |
| 性质  | 体检——网站本身有没有毛病                                   | 看病——业务结果出问题了为什么                             |
| 看什么 | SSL、死链、SEO 标签、内容重复度、CTA 有没有                     | 留资、UV、跳出率、渠道转化                              |
| 产出  | 健康度分 + 问题列表 + 修复任务                              | 三类变化原因 + 应对产出（异常→措施 / 提升→夸赞 / 持平→建议）+ 异常类执行 |
| 触发  | 客户点"重新检测"按钮                                     | 定时                                          |
| 频率  | 低频（周/月）                                         | 高频（日/周）                                     |
| 现状  | 已实现（`growth-engine` 的 scanning/report/tasks 流程） | 本方案要新建                                      |


**关键约束**：线 B 不复用线 A 的"重新检测"入口，也不复用线 A 的 6 维度检测流程。两条线的结果可汇入同一"智能体任务"队列，但分析逻辑独立。线 B 通过 `query_health_status` 读线 A 结果作为归因输入之一，但不依赖线 A 的检测流程。

### 1.4 目标与非目标

**目标（本方案覆盖）：**

- 定义归因 Agent 分析业务指标的方法论（6 个模块）；
- 明确归因 Agent 在整体架构中的定位与调度关系；
- 明确指标体系（模块 1）：哪些指标进 Agent 视野、怎么进、以什么形式进；
- 定义 6 段转化漏斗作为归因骨架，贯穿模块 3-4。

**非目标（本方案不覆盖）：**

- 引流侧（SEO/GEO/社媒内容生成）的优化策略本身（那是 AI 内容引擎的执行侧）；
- 网站健康度 6 维度体检的检测项调整（线 A）；
- 神策埋点方案（见独立埋点文档）；
- L4 其他 4 个产品模块的内部实现（本方案只定义"调度去向"，不定义它们怎么做）。

---



## 2. 用户与场景



### 2.1 用户角色

**使用者**：B 端客户（网站所有者）+ 平台运营人员 + 售前销售。

**服务方式**：所有客户统一走同一套归因 Agent 能力——归因结论 + 措施直接推给客户，按执行边界（自动执行 / 确认后执行 / 只出方案）落地。平台运营可协助客户理解报告与确认措施，但不因客户层级区分 Agent 能力范围。

**数据视角**：归因以**该站点自身的业务指标**为准（本站历史对比、渠道间对比、页面间对比），不按平台整体平均转化率判断——整体平均对新站点失真，对运营无指导意义。

### 2.2 触发方式（定时 · 周期可配）

归因 Agent **只走定时触发**，不设手动/实时/事件触发。客户不用自己找问题，AI 主动定期给报告——这更符合"AI 主动干活"的定位，也省掉监控层与事件接入的架构成本。


| 项    | 设计                                                   |
| ---- | ---------------------------------------------------- |
| 触发方式 | 定时自动跑全量指标判定                                          |
| 周期配置 | 客户在**站点设置**中配置触发周期，**最短 7 天**，可设 7/14/30 天或自定义（≥7 天） |
| 默认周期 | 7 天（一周一次）                                            |
| 配置入口 | 站点设置 → 归因分析周期                                        |
| 跑完产出 | 一份"指标变化报告"进智能体任务队列，客户进工作台看红点                         |


**为什么只定时、不做手动/实时**：

- 手动触发会让客户养成"自己找问题"的习惯，与"AI 主动干活"定位冲突；
- 实时/事件触发要先建监控层 + 事件接入，架构成本高，且 T+1 日更数据下"实时"是伪实时；
- 定时周期可配（7 天+），已能覆盖所有场景，客户想更勤就设 7 天，想省事就设 30 天。



### 2.3 用户体验闭环

**核心原则**（对齐老板会议）：客户看到的要少，聚焦"结果 + 为什么"，不聚焦"怎么做"。**不只报问题，也报成绩**——让客户看到 AI 持续在干活、干活有价值。

```text
定时跑完 → 推送"指标变化报告"（工作台红点）
  → 客户进工作台看报告，每条变化含三类之一：
    ├─ 异常（下降）→ 归因结论 + 措施清单（带依据卡片）→ 确认执行/自动执行/只出方案 → 复盘
    ├─ 提升（上升）→ 归因结论（为什么涨）+ 夸赞（做得好的原因）→ 无需执行
    └─ 持平（平稳）→ 归因结论（为什么没动）+ 提升建议（只出方案，不执行）
  → 回到主视图
```

**三类变化的展示**：

- **异常**：红色卡，带措施依据卡片，引导确认执行；
- **提升**：绿色卡，归因 + 夸赞（"本周留资率提升 0.5pp，主要因 SEO 内容优化见效，继续保持"）；
- **持平**：灰色卡，归因 + 说明 + 可选的提升建议。

**给客户看的 vs 不给客户看的**：

- **给客户看**：三类变化信号、归因结论（折叠）、措施依据卡片、执行结果、复盘指标变化、夸赞与说明。
- **不给客户看**：归因下钻的原始数据序列、模型兜底判定细节、调度 skill 名称、规则阈值。这些给 Agent + 运营看。

**单页面闭环**：看报告 → 归因 → 策略 → 执行 → 复盘在一个页面流内完成，避免频繁切换菜单（对齐老板会议明确要求）。诊断报告可作为独立结果页单独查看，但闭环主流程不跳走。

---



## 3. 产品架构与数据流



### 3.1 六模块总览与数据流向

```text
模块1 指标体系    →  模块2 指标变化发现  →  模块3 归因分析  →  模块4 应对产出  →  模块5 执行  →  模块6 复盘
（看什么）          （什么算变化·三类）     （为什么变化）     （怎么办/怎么夸）   （谁去干）      （效果如何）
   │                    │                      │                 │                │              │
   └─ 数据 tool 拉数     └─ 定时触发             └─ 产出根因       └─ 按类型产出     └─ 只执行异常类  └─ 沉淀回知识库
                        （异常/提升/持平）                         异常→措施        提升持平不执行
                                                                  提升→夸赞
                                                                  持平→建议
                                                                                            ↓
                                                                                       知识库越用越厚
                                                                                            ↓
                                                                                   下一轮归因更准（飞轮回流）
```

**数据流**：模块 1 给数据 → 模块 2 定时判定三类变化（异常/提升/持平）→ 模块 3 对三类都归因定位根因 → 模块 4 按类型产出（异常给措施、提升给夸赞、持平给建议）→ 模块 5 **只对异常类**调度执行 → 模块 6 复盘异常类执行效果 + 三类归因都沉淀 → 回流知识库 → 下一轮归因更准。这是飞轮的"分析 → 优化"环节。

**关键**：模块 2 从"只看异常"扩展为"看三类变化"，让客户每次收到的不只是"哪里坏了"，还有"哪里好了、为什么好"——AI 的价值可见。模块 5/6 只在异常类介入，提升/持平类止于"归因 + 展示"。

### 3.2 与 L4 其他模块的调度关系

归因 Agent（本方案）是 L4 的"大脑"，模块 5 执行时**调度 L4 其他 4 个产品模块**，不新建执行能力：


| 措施类型                     | 调度的 L4 模块       | 说明                   |
| ------------------------ | --------------- | -------------------- |
| 落地页/营销页内容优化              | **AI 内容引擎**     | 按搜索意图重写/生成落地页        |
| 产品详情页内容优化                | **AI 内容引擎**     | 按行业规范重写详情页、补结构化数据    |
| CTA 时机/位置调整、心理学触发器       | **转化路径设计器**     | CTA 位置移动、A/B 测试变体    |
| 表单字段精简、分步填写、进度条          | **智能表单系统**      | 表单优化（二期能力，一期只出方案）    |
| 客服话术/知识库配置问题             | **AI 智能客服 PRO** | 暴露知识库风险，引导补 FAQ/上传文档 |
| 技术止血（404/SSL/sitemap/链接） | **工具/Agent 直调** | 不走内容 skill，直接调工具修复   |
| 涉及钱/外部（预算/外链/申诉）         | **只出方案**        | 不调度，客户自行处理           |


**关键**：一期智能表单系统未上线，表单类措施一期只出方案；其余 4 个调度去向一期均落地。

### 3.3 核心骨架：6 段转化漏斗

归因的骨架是 **6 段转化漏斗**（对齐架构设计文档 1.3.2，并按真实 B2B 访客路径修订）。这 6 段贯穿模块 3（归因定位）和模块 4（措施映射）。

```text
① 渠道到达 → ② 落地页 → ③ 站内浏览 → ④ 转化入口触发 → ⑤ 转化交互 → ⑥ 成功留资
  (100%)      (落地页)    (多页/深度)    (CTA/客服)      (表单/对话)    (提交/留资)
```


| 段 | 名称 | 定义 | 第一层信号（体检必看） | 第二层下钻（按需调取，对齐模块 1 · 4.2/4.3） | 标准流失原因 | 调度去向 |
| --- | --- | --- | --- | --- | --- | --- |
| ① | **渠道到达** | 访客从各渠道（广告/SEO/社媒/直接/外链）进入网站 | UV、PV；各渠道 UV | **渠道/页面明细**：各渠道 4 指标明细（UV/留资数/留资率/跳出率）；**漏斗留存率**：渠道到达→落地页；**意图数据**：SEO/广告来路搜索关键词 | 流量不精准/渠道匹配差 | AI 内容引擎（SEO 内容）、广告侧 |
| ② | **落地页** | 落地的第一个具体页面（首页/产品详情页/营销页/关于我们等，不限于首屏） | 跳出率 | **渠道/页面明细**：平均停留时长、页面跳出明细；**行为埋点·页面浏览**：滚动深度；**漏斗留存率**：落地页→站内浏览；**意图数据**：搜索关键词、站内搜索词 | 内容不匹配搜索意图/加载慢/首屏价值弱 | AI 内容引擎（落地页改写）、转化路径设计器 |
| ③ | **站内浏览** | 落地后的浏览行为（可能落地页就走，也可能浏览多页：关于我们/产品列表/产品详情） | 无独立第一层信号（由「UV/跳出正常 + 留资跌」间接指向本段） | **渠道/页面明细**：访问深度（人均页数）、页面跳转；**行为埋点·页面浏览**：多页浏览路径/停留；**漏斗留存率**：站内浏览→转化入口触发；**意图数据**：站内搜索词 | 内容不吸引/站内导航差/没找到信息 | AI 内容引擎、转化路径设计器（导航/内链） |
| ④ | **转化入口触发** | 产生转化意向——点 CTA，**或主动找智能客服聊天** | 无独立第一层信号（由「前段正常 + 留资跌」间接指向本段） | **行为埋点·点击行为**：CTA 曝光/点击；**漏斗留存率**：站内浏览→转化入口触发；**意图数据**：客服对话意图（主动发起占比） | CTA 不明显/时机不对/没触发主动对话 | 转化路径设计器（CTA 时机/位置）、智能客服 PRO（主动对话策略） |
| ⑤ | **转化交互** | 进入转化通道后的交互——**表单通道**（填了/没填就走）**或客服通道**（聊了会儿就走/聊完留资），两条并行 | 无独立第一层信号（由「入口触发正常 + 留资跌」间接指向本段） | **行为埋点·交互行为**：表单填写进度、客服对话内容；**漏斗留存率**：转化入口触发→转化交互；**意图数据**：客服对话意图聚类与转化表现 | 表单太复杂/客服回答不专业/信任感不足/等待焦虑 | 智能表单系统、智能客服 PRO（话术/知识库） |
| ⑥ | **成功留资** | 最终成功提交表单或通过客服完成留资 | 留资数、留资率；页面转化率排行（TOP5 最差/跌幅） | **行为埋点·转化行为**：留资字段内容、来源渠道；**漏斗留存率**：转化交互→成功留资；**渠道/页面明细**：页面转化率全排行 | 信任感不足/最后一步放弃 | 线索管理（线索质量评分） |


这 6 段每段都能下钻、都能归因、都能调度，是模块 3-4 的贯穿骨架。

> **读表说明**：第一层信号 = 模块 1「第一层 · 异常探测层」里与本段直接相关的子集；第二层下钻 = 模块 1「第二层 · 归因下钻层」四类数据（渠道/页面明细、4 类行为埋点、漏斗留存率、用户意图数据）里本段会用到的子集——**两边同名同口径，不另造指标**。另：健康度（第一层第 5 类）是技术横切面，不属于任何漏斗段，但会干扰落地页等段的归因，体检时必看。

---



## 4. 模块 1 · 指标体系（看什么）



### 4.1 设计原则与三层结构

**设计原则**：

1. **进 Agent 视野的不是"图"，是"数据"。** 图是给人看的，Agent 要的是结构化数值 + 时间序列。
2. **不是全进，也不是只进北极星，要按分析链路分层。** 全进噪音太大；只进北极星分析不下去。
3. **必须带时间序列，不只是快照。** 没有时间序列，变化判定只能靠死阈值，那是规则引擎，不是 AI。
4. **口径统一。** 所有指标在 Agent 视野里必须与看板同一口径、同一数据源，禁止二次加工产生歧义。

**三层结构**：

```text
第一层 · 异常探测层   → Agent 每次必看，判断"有没有事"
第二层 · 归因下钻层   → 发现异常后按需调取，定位"事在哪、为什么"
第三层 · 不进 Agent   → 给人看，Agent 不分析
```



### 4.2 第一层 · 异常探测层（Agent 必看最小集）

6 类指标，每次分析都拿。少一个，Agent 就会瞎一块。

#### 1) 北极星指标（判断"有没有事"）


| 指标  | 口径                | 时间序列要求  |
| --- | ----------------- | ------- |
| 留资数 | 周期内成功提交询盘/留资的去重条数 | 30 天日序列 |
| 留资率 | 留资数 / UV          | 30 天日序列 |


**为什么必进**：最终业务结果，它异常才触发整个分析链路。它不异常，其他都不用看。

#### 2) 流量段指标（判断"是流量问题还是转化问题"）


| 指标  | 口径       | 时间序列要求  |
| --- | -------- | ------- |
| UV  | 周期内独立访客数 | 30 天日序列 |
| PV  | 周期内页面浏览量 | 30 天日序列 |


**为什么必进**：留资跌了，先看 UV 跌没跌。UV 跌 = 流量侧问题；UV 没跌 = 承接/转化侧问题。这是归因的第一道分叉。

#### 3) 承接段指标（判断"来了有没有接住"）


| 指标  | 口径                    | 时间序列要求  |
| --- | --------------------- | ------- |
| 跳出率 | 只访问 1 个页面就离开的会话 / 总会话 | 30 天日序列 |


**为什么必进**：UV 没跌但留资跌，多半是承接出了问题。跳出率是承接段最敏感的信号。

#### 4) 渠道维度指标（判断"哪个渠道异常"）


| 指标     | 口径              | 时间序列要求      |
| ------ | --------------- | ----------- |
| 各渠道 UV | 按渠道拆分的 UV       | 30 天日序列，按渠道 |
| 各渠道留资率 | 各渠道留资数 / 各渠道 UV | 30 天日序列，按渠道 |


**渠道枚举**（固定，新增时显式通知 Agent）：广告投放 / 社交媒体 / 外贸 SEO / 直接访问 / 外链。

**为什么必进**：总留资率跌，可能是某一个渠道拖累。不下钻到渠道，归因就停在"留资跌了"这种废话结论。

#### 5) 网站健康度（判断"是不是网站本身有毛病"）


| 指标      | 口径                   | 时间序列要求    |
| ------- | -------------------- | --------- |
| 健康度分    | 综合健康评分（0-100）        | 当前值 + 上次值 |
| 待处理问题数  | 未处理的健康问题条数           | 当前值       |
| 待处理问题列表 | 每条问题的类型 + 严重度 + 关联页面 | 当前列表      |


**为什么必进**：技术问题（404、加载慢、死链）会直接拉低承接和转化。Agent 必须知道"有没有技术因素在捣乱"，否则会归因到内容上，给错方案。

**关键**：不能只给分数，必须给**问题列表**——Agent 要能判断"这 N 个问题里，哪个跟当前异常相关"。数据来源对接线 A 的健康度检测结果。

#### 6) 页面转化率排行（判断"哪个页面在拖后腿"）


| 指标           | 口径                         | 时间序列要求      |
| ------------ | -------------------------- | ----------- |
| 转化率最差 TOP5   | 留资率最低的 5 个页面（UV > 阈值，过滤噪音） | 当前值 + 7 天前值 |
| 转化率跌幅最大 TOP5 | 留资率环比跌幅最大的 5 个页面           | 当前值 + 上周期值  |


**为什么必进**：Agent 该盯的是问题，不是表彰。只看 TOP5 最好的会漏掉真正该处理的页面。

**过滤规则**：UV 太小的页面（如日 UV < 10）转化率波动大、无统计意义，必须设 UV 下限过滤，否则 Agent 会被噪音带偏。**UV 下限由产品定，默认值 10，可配置。**

### 4.3 第二层 · 归因下钻层（按需调取）

**平时不调，第一层发现异常后才调。全量塞给 Agent 会噪音太大。**

第二层分四类数据，按异常类型按需调取：

#### 4.3.1 渠道/页面明细（承接模块 3 下钻用）


| 指标             | 调取时机                    | 口径                       |
| -------------- | ----------------------- | ------------------------ |
| 各渠道 4 指标明细     | 北极星异常，需下钻渠道时            | UV / 留资数 / 留资率 / 跳出率，按渠道 |
| 页面转化率全排行       | 渠道定位后，需下钻到页面时           | 全页面转化率，含最好最差             |
| 平均停留时长         | 跳出率异常，判断"内容不行"还是"流量不精准" | 按页面/渠道                   |
| 访问深度（人均页数）     | 同上                      | 人均 PV                    |
| 7 天 / 30 天时间序列 | 判断"突变"还是"趋势性下滑"         | 第一层各指标的完整日序列             |




#### 4.3.2 4 类行为埋点数据（归因下钻核心原料，新增）

对齐架构设计文档 1.3.2「数据采集」，4 类行为是归因下钻到"具体访客为什么流失/为什么留资"的核心原料。**不是每次全拉，是归因到某段漏斗时按需调取该段的行为数据。**


| 行为类      | 采集内容                 | 调取时机                  |
| -------- | -------------------- | --------------------- |
| **页面浏览** | 浏览了哪些页面、停留时长、滚动深度    | 归因到②落地页/③站内浏览段，判断内容承接 |
| **点击行为** | 点击了哪些按钮、链接、图片        | 归因到④转化入口段，判断 CTA 触达   |
| **交互行为** | 表单填写进度、鼠标悬停区域、客服对话内容 | 归因到⑤转化交互段，判断转化摩擦      |
| **转化行为** | 是否留资、留资字段、来源渠道       | 归因到⑥成功留资段，判断留资质量      |


**关键约束**：4 类行为进 Agent 视野（归因下钻层），但**不进数据看板给客户看**（第三层）。看板只放聚合后的精简数（人均页数/平均停留/CTA 触达率/表单启动率），原始行为序列只给 Agent + 运营侧看。

#### 4.3.3 6 段漏斗各段留存率（归因定位骨架）

对齐第 3.3 节 6 段漏斗，每段的留存率与流失率是模块 3"定位漏斗段"的直接输入：


| 漏斗段 | 留存率口径 | 流失率口径 |
| ----- | ------- | ------- |
| 渠道到达 → 落地页 | 进入网站且未立即跳出的会话占比 | 跳出率（落地页段） |
| 落地页 → 站内浏览 | 未跳出且浏览≥2 页的会话占比 | 落地页跳出率 |
| 站内浏览 → 转化入口触发 | 触发 CTA 或主动客服的会话占比 | 未触发转化入口率 |
| 转化入口触发 → 转化交互 | 进入表单填写或客服对话的会话占比 | 入口触发后未交互率 |
| 转化交互 → 成功留资 | 成功提交表单或客服留资的会话占比 | 转化交互流失率 |


**调取时机**：留资率异常，需定位"哪段漏最多"时调取（对应 `query_funnel_dropoff`）。

**调取逻辑**：Agent 先看第一层 → 判断异常类型 → 决定调第二层哪个 → 调了再分析。**不是一次性全给。**

#### 4.3.4 用户意图数据（意图归因核心原料，新增）

对齐老板架构 L3「搜索意图精准匹配」。4 类行为埋点告诉 Agent"访客做了什么"，意图数据告诉 Agent"**访客想要什么**"——行为是结果，意图是动机。归因到落地页/站内浏览段时，意图数据能把"内容不匹配"从模糊判断升级为"**哪个意图、错配到哪页**"的精确结论。三类来源：

| 意图数据源 | 具体口径 | 调取时机 |
| --------- | ------- | ------- |
| **搜索关键词** | SEO 渠道：来路搜索词（GSC/百度站长对接）+ 各词展现/点击/落地页；广告渠道：投放关键词 + 广告创意文案 | 归因落地页段，判断"搜索词意图 vs 落地页内容"是否错配 |
| **站内搜索词** | 站内搜索的关键词、搜索次数、0 结果率、搜索后跳出率 | 归因落地页/站内浏览段，发现"内容缺口"（访客想要但网站没有的内容） |
| **客服对话意图** | 智能客服对话内容聚类出的意图标签（价格咨询/产品对比/售后/案例考察等）+ 各意图占比与转化率 | 归因转化入口触发/转化交互段，分析"哪类意图的访客转化好/差" |

**三个典型用法**：

1. **意图×落地页精确错配**：如"搜'CNC 加工价格'的访客落到品牌故事页"→ 比"内容不匹配"具体得多，措施也更精确；
2. **内容缺口发现**：站内搜索 0 结果率高的词 = 访客想要但网站没有的内容 → 调度 AI 内容引擎生成，是内容选题的金矿；
3. **意图聚类×转化表现**：哪类意图转化好、哪类差、差在哪段 → 反哺 L3 意图匹配能力。

**数据归属说明**：意图理解能力（意图识别、聚类、匹配）是 L3「搜索意图精准匹配」的活，本模块不重复建设；归因 Agent 是**意图数据的使用方**（拿意图做归因）+ **反哺方**（归因发现的新意图/意图转化表现回流 L3）。

### 4.4 第三层 · 不进 Agent 视野


| 不进的东西          | 为什么不进                    |
| -------------- | ------------------------ |
| 趋势折线图 / 饼图本身   | 图给人看，Agent 要的是数值序列       |
| "转化洞察"文字框      | 这是上次 Agent 的产出，作为输入会循环   |
| 健康度的可视化呈现      | Agent 只要分数 + 问题列表        |
| PV/UV 之外的纯展示指标 | 如"访问时段分布"等，归因用不上         |
| 4 类行为原始序列      | 给 Agent + 运营侧，不给客户看板 |




### 4.5 数据接入要求（给研发的硬约束）

本节是模块 1 能否落地的关键，不是可选项。

**要求 1 · 必须带时间序列，不只是快照**：第一层所有指标，必须以"过去 30 天日序列"形式给 Agent，不能只给"当前值 + 环比"。没有时间序列 → 变化判定只能靠死阈值 → 那是规则引擎，不是 AI。

**要求 2 · 刷新频率**：第一层日更（T+1）；第二层按需调取时实时；健康度每次检测后更新（对接线 A）。

**要求 3 · 口径统一**：所有指标在 Agent 视野里必须与数据看板同一口径、同一数据源，建议 Agent 直接调看板同一数据接口，不要二次加工。

**要求 4 · 渠道枚举固定**：渠道列表枚举固定。新增渠道时显式通知 Agent（配置项），否则 Agent 会把新渠道当异常处理。

**要求 5 · 页面排行 UV 过滤**：页面转化率排行必须带 UV 下限过滤参数，默认 10，可配置。

**要求 6 · 4 类行为埋点接入（新增）**：4 类行为埋点数据必须可按漏斗段 + 时间范围调取，供归因下钻用。埋点方案见独立埋点文档，本方案只定义"调取接口约束"。

**要求 7 · 意图数据接入（新增）**：搜索关键词（GSC/百度站长/广告平台对接）、站内搜索词（含 0 结果率）、客服对话意图标签（由 L3 意图能力聚类产出），必须可按时间范围 + 漏斗段调取。意图识别与聚类由 L3「搜索意图精准匹配」提供，本模块只定义"调取接口约束"，不重复建设意图理解能力。

### 4.6 指标一览表（给老板看的）


| 层       | 指标                                               | 谁看        | 何时看     |
| ------- | ------------------------------------------------ | --------- | ------- |
| **第一层** | 留资数/率、UV/PV、跳出率、各渠道 UV+留资率、健康度+问题列表、页面最差/跌幅 TOP5 | Agent     | 每次必看    |
| **第二层** | 渠道明细、页面全排行、停留时长、访问深度、4 类行为埋点、6 段漏斗留存、用户意图数据   | Agent     | 发现异常后按需 |
| **第三层** | 折线图、饼图、洞察文字、可视化、4 类行为原始序列                        | 人（看板展示） | 看板展示    |




### 4.7 模块 1 边界声明

模块 1 只定义"看什么"。**不定义**："怎么算异常"（模块 2）、"怎么归因"（模块 3）、"怎么处理"（模块 4-5）、"怎么复盘"（模块 6）。

模块 1 的交付物：第 4.2-4.5 节的指标清单 + 数据接入要求。

---



## 5. 模块 2 · 指标变化发现



### 5.1 模块定位

模块 2 回答两个问题：**什么算指标变化**、**什么时候启动归因分析**。它判定的不只是"异常（下降）"，还有"提升（上升）"和"持平（平稳）"——三类变化都触发归因，让客户既看到问题也看到成绩。它是模块 3（归因）的触发器。

```text
模块1 给数据 → 模块2 定时判三类变化（异常/提升/持平） → 触发模块3 归因 → 模块4 按类型产出 → 模块5 只执行异常类 → 模块6 复盘
```

**三类变化定义**：

- **异常（下降）**：指标跌破规则阈值/环比跌超阈值/连续下滑——红色，需处理；
- **提升（上升）**：指标突破正向阈值/环比涨超阈值/连续上升——绿色，夸赞；
- **持平（平稳）**：指标连续 N 期无变化（在正常区间内但不动）——灰色，说明 + 提升建议。

**为什么不只看异常**：只报问题不报成绩，客户感受不到 AI 在持续工作。提升要夸赞（让客户知道哪里做对了）、持平要说明（让客户理解为什么没动），AI 的价值才可见。

### 5.2 变化判定三层架构

```text
┌─ 规则层（为主，覆盖 80% 可解释变化）
│    阈值规则 · 变化规则 · 趋势规则（含正向 + 负向 + 持平）
│    ↓ 命中即标记变化类型（异常/提升/持平），可解释、可调
│
├─ 模型兜底层（补 20% 规则漏的）
│    形态异常 · 联动异常 · 结构异常
│    ↓ 规则没触发但"感觉不对"时介入
│
└─ 沉淀闭环
     模型兜底命中的变化 → 人工标注确认 → 沉淀成新规则 → 越用越准
```

**为什么是这三层**：只有规则层 → 死板，漏掉"没跌破阈值但形态已坏"的；只有模型层 → 黑盒，老板问"为啥报警"答不上来；规则为主 + 模型兜底 + 沉淀闭环 → 80% 可解释，20% 不漏，且越用规则越多、模型越少介入。

**规则层覆盖三类变化**：阈值/变化/趋势规则既判负向（异常）也判正向（提升）也判持平，一套规则三种产出。

### 5.3 规则层详细规则（按模块 1 的 6 类指标，覆盖三类变化）

**规则类型定义**：


| 规则类型 | 定义                | 例子                           |
| ---- | ----------------- | ---------------------------- |
| 阈值规则 | 绝对值跌破/突破某线        | 留资率 < 1%（异常）/ 留资率 > 5%（提升）   |
| 变化规则 | 环比/同比变化超阈值（含正向负向） | 留资环比跌 30%（异常）/ 留资环比涨 30%（提升） |
| 趋势规则 | 连续 N 期同向变化        | 连续 5 期下滑（异常）/ 连续 3 期上升（提升）   |
| 持平规则 | 连续 N 期在正常区间内但无变化  | 留资率连续 4 期维持 3.0%±0.1pp（持平）   |


**各指标适用规则与默认阈值**：


| 指标           | 适用规则     | 默认阈值（可配置）                    | 异常方向      | 提升方向             | 持平判定       |
| ------------ | -------- | ---------------------------- | --------- | ---------------- | ---------- |
| 留资数          | 阈值+变化+趋势 | 阈值：<5/周；变化：环比跌≥30%；趋势：连续5期下滑 | ↓ 异常      | 环比涨≥30% 或 连续3期上升 | 连续4期±10%   |
| 留资率          | 阈值+变化    | 阈值：<1%；变化：环比跌≥0.5pp          | ↓ 异常      | 环比涨≥0.5pp        | 连续4期±0.1pp |
| UV           | 变化+趋势    | 变化：环比跌≥30%；趋势：连续5期下滑         | ↓ 异常      | 环比涨≥30%          | 连续4期±10%   |
| PV           | 变化       | 变化：环比跌≥30%                   | ↓ 异常      | 环比涨≥30%          | 连续4期±10%   |
| 跳出率          | 阈值+变化    | 阈值：>70%；变化：环比升≥10pp          | ↑ 异常（升=坏） | 环比降≥10pp         | 连续4期±2pp   |
| 各渠道 UV       | 变化       | 环比跌≥40%（渠道波动大，阈值更宽）          | ↓ 异常      | 环比涨≥40%          | 连续4期±15%   |
| 各渠道留资率       | 变化       | 环比跌≥1pp                      | ↓ 异常      | 环比涨≥1pp          | 连续4期±0.2pp |
| 健康度分         | 阈值+变化    | 阈值：<60；变化：环比跌≥10分            | ↓ 异常      | 环比升≥10分          | —          |
| 待处理问题数       | 阈值       | 新增 P0 问题≥1                   | ↑ 异常      | P0 问题清零          | —          |
| 页面转化率跌幅 TOP5 | 规则即定义    | 入榜即异常信号                      | 跌幅大=异常    | —                | —          |
| 页面转化率涨幅 TOP5 | 规则即定义    | 入榜即提升信号                      | —         | 涨幅大=提升           | —          |


**阈值设计原则**：

- 所有阈值**可配置**，默认值是起点，不是圣旨；
- **一期阈值统一**（不分外贸/内贸站点类型），二期再分站点配置；
- **小流量站点放宽阈值**：按 UV 分档，UV < 200 用宽阈值（阈值放宽 1.5 倍），UV ≥ 200 用标准阈值。避免小流量站点天天误报；
- **正向阈值可比负向略宽**：避免轻微波动被当"提升"夸赞，提升判定从严（如留资率涨≥0.5pp 才算提升，避免噪音）；
- **持平需连续 N 期**：单期不动不算持平，连续 4 期（约一个月）无变化才标记持平。



### 5.4 模型兜底层（抓规则漏的 20%）

规则层没命中，但以下三类"感觉不对"的，模型介入：


| 兜底类型 | 抓什么            | 怎么判                      |
| ---- | -------------- | ------------------------ |
| 形态异常 | 曲线形态变坏但没跌破阈值   | 统计模型：波动率突增、周末效应消失、平台期破位  |
| 联动异常 | 单指标没异常但多指标联动异常 | 规则组合：UV升+留资降+跳出升 = 流量变垃圾 |
| 结构异常 | 总量没变但结构突变      | 渠道占比突变（某渠道从40%掉到20%）     |


**兜底命中后的处理（沉淀闭环）**：

```text
模型兜底命中 → 生成"模型兜底异常"（标注模型判定）
  → 人工标注：真异常 or 误报
    → 真异常 → 沉淀成新规则（下次同类直接规则命中）
    → 误报 → 标记负样本，优化模型
```

**关键约束**：模型兜底产出的异常，**必须标注"模型判定"**，与规则异常区分。沉淀门槛：必须经人工标注确认才沉淀成规则，避免误报污染判定逻辑。

### 5.5 变化分级（对接模块 5 执行边界）

**异常类分级**（对接模块 5 执行边界）：


| 级别     | 触发条件                | 处理优先级     | 对接模块 5 |
| ------ | ------------------- | --------- | ------ |
| **P0** | 北极星异常（留资数/率触发任意规则）  | 必须处理，优先调度 | 见模块 5  |
| **P1** | 过程指标异常（UV/跳出率/渠道异常） | 建议处理      | 见模块 5  |
| **P2** | 健康度/页面异常            | 可处理       | 见模块 5  |


**分级逻辑**：越靠近最终业务结果（留资），级别越高。流量段异常是 P1（流量跌未必影响留资）；留资跌是 P0（这就是结果）。

**提升/持平类不分级**：

- **提升类**：不设 P 级，统一标记 `changeType: 'up'`，进模块 4 走"归因 + 夸赞"，不进模块 5 执行；
- **持平类**：不设 P 级，统一标记 `changeType: 'flat'`，进模块 4 走"归因 + 说明 + 提升建议（只出方案）"，不进模块 5 执行。

**关键**：只有异常类（`changeType: 'down'`）才进模块 5 调度执行，提升/持平止于模块 4 的归因与展示。

### 5.6 触发方式（定时 · 周期可配，详见第 2.2 节）

触发方式已在第 2.2 节定义：**只走定时触发，周期在站点设置配置，最短 7 天**。本节补充落地细节：


| 项    | 设计                                                          |
| ---- | ----------------------------------------------------------- |
| 触发方式 | 定时自动跑全量指标判定（异常/提升/持平三类一起判）                                  |
| 周期配置 | 站点设置 → 归因分析周期，可设 7/14/30 天或自定义（≥7 天）                        |
| 默认周期 | 7 天                                                         |
| 配置存储 | 对接 `AutoDetectConfig`，新增 `analysisPeriodDays` 字段（默认 7，最小 7） |
| 跑完产出 | 一份"指标变化报告"进智能体任务队列，含三类变化条目                                  |


**定时的具体设计**：按客户配置的周期定时跑；跑完生成"本期指标变化报告"进智能体任务队列，含异常条目（红）、提升条目（绿）、持平条目（灰）；客户进工作台看红点逐条查看。

**为什么不设手动/实时/事件触发**：见第 2.2 节——手动让客户养成自己找问题的习惯，与"AI 主动干活"冲突；实时/事件要先建监控层 + 事件接入，架构成本高，T+1 日更下"实时"是伪实时。

### 5.7 变化任务过期机制

- 变化任务进队列后，**一个周期内未处理自动关闭**（状态置 `expired`，周期 = 客户配置的触发周期）；
- 关闭时记录"未处理原因"（可选，供运营复盘队列堆积问题）；
- 避免变化任务无限堆积，污染队列。



### 5.8 变化产出结构（进智能体任务队列）

每条变化生成一个"归因分析任务"，字段：

```text
DiagnosisTask {
  taskType: 'diagnosis',
  triggerType: 'scheduled',                          // 只留定时
  analysisPeriodDays: number,                         // 客户配置的周期（7/14/30/自定义）
  changeType: 'down' | 'up' | 'flat',                 // 变化类型：异常/提升/持平（新增）
  changedMetric: '留资率',
  changeDirection: 'down' | 'up' | 'flat',
  changedValue: '2.7%',
  baselineValue: '3.2%',
  ruleType: 'threshold' | 'change' | 'trend' | 'flat' | 'model_form' | 'model_linkage' | 'model_structure',
  severity: 'P0' | 'P1' | 'P2' | null,                // 异常类有P级，提升/持平为null
  isModelBased: bool,
  triggeredAt: timestamp,
  expiresAt: timestamp,
  siteId: string,
  metricSnapshot: {...},
  status: 'pending_attribution'
}
```

**关键**：变化任务产出后状态是 `pending_attribution`，等模块 3 接手归因（三类都归因）。模块 2 只负责"发现并登记三类变化"，不负责"分析为什么"。`changeType` 决定后续走哪条产出链：`down` → 模块4措施+模块5执行；`up` → 模块4夸赞；`flat` → 模块4说明+建议。

### 5.9 模块 2 边界声明

模块 2 只定义"什么算变化（异常/提升/持平）+ 什么时候触发"。**不定义**："为什么变化"（模块 3）、"怎么办/怎么夸"（模块 4）、"怎么执行"（模块 5）、"怎么复盘"（模块 6）。

模块 2 的交付物：第 5.3-5.8 节的规则清单（含正向/持平）+ 兜底设计 + 触发机制（定时周期可配）+ 过期机制 + 变化产出结构。

---



## 6. 模块 3 · 归因分析



### 6.1 模块定位

模块 3 回答：**为什么变化**。它接模块 2 的三类变化信号（异常/提升/持平），下钻定位根因，产出"原因 + 置信度 + 验证依据"，交给模块 4 按类型产出（异常给措施、提升给夸赞、持平给说明+建议）。

```text
模块2 给三类变化信号 → 模块3 对三类都归因定位根因 → 模块4 按类型产出
```

**模块 3 对三类变化一视同仁**：异常要归因（为什么跌），提升也要归因（为什么涨，让客户知道哪里做对了），持平也要归因（为什么没动，让客户理解现状）。归因深度三类一致（都下钻到根因 + 置信度，见第 6.5 节）。

**模块 3 不生成方案/夸赞/建议、不执行，只回答"为什么"。** 越界就会和模块 4 糊在一起，导致职责不清、无法独立评估。

### 6.2 归因框架：三步走

```text
第一步 · 定位漏斗段   → 变化发生在 6 段漏斗的哪一段（第 3.3 节骨架）
第二步 · 下钻维度     → 在变化段里，是哪个渠道/页面/设备/地域/时段驱动/拖累
第三步 · 假设验证     → 生成假设 → 调数据验证 → 区分真因 vs 相关
```

这三步是**顺序执行**，不是并行。跳步会导致归因停在表面（"留资跌了"是废话，"广告渠道落地页承接变差导致留资跌"才是根因）。

### 6.3 第一步 · 定位漏斗段（对齐 6 段漏斗，三类变化都定位）

变化进来后，先判断它发生在 6 段漏斗（第 3.3 节）的哪一段。每段对应模块 1 的指标。下表以**异常（下降）**为例列判定逻辑，提升/持平的定位逻辑类似（看"哪段涨了"/"哪段没动"）：


| 漏斗段      | 对应指标            | 变化含义（异常为例） | 大致归因方向           |
| -------- | --------------- | ---------- | ---------------- |
| ① 渠道到达   | UV / PV 异常      | 来的人少了      | 流量侧（渠道、投放、SEO）   |
| ② 落地页    | 跳出率异常（落地页段）     | 来了没接住      | 落地页内容、加载、首屏价值    |
| ③ 站内浏览   | 平均页数/停留异常       | 接住了没深看     | 站内导航、内容吸引力       |
| ④ 转化入口触发 | CTA 点击率/客服发起率异常 | 看了没行动      | CTA 时机/位置、主动对话策略 |
| ⑤ 转化交互   | 表单完成率/客服对话转化率异常 | 行动了没完成     | 表单复杂度、客服专业度、信任感  |
| ⑥ 成功留资   | 留资率/留资字段质量异常    | 完成了质量差     | 信任感、最后一步放弃、线索质量  |


**异常类关键判定逻辑**（基于第一层指标做第一道分叉）：

- 留资跌 + UV 跌 → ①渠道到达段问题（先治流量）
- 留资跌 + UV 没跌 + 跳出率升 → ②落地页段问题（治落地页）
- 留资跌 + UV 没跌 + 跳出率没升 + 平均页数跌 → ③站内浏览段问题（治站内导航/内容）
- 留资跌 + UV/跳出/页数都正常 + CTA 点击率低 → ④转化入口段问题（治 CTA/主动对话）
- 留资跌 + CTA 正常 + 表单完成率/客服转化率低 → ⑤转化交互段问题（治表单/客服）
- 留资量正常 + 留资字段质量差 → ⑥成功留资段问题（治信任感/线索质量）

**提升类定位**：看"哪段变好了"——如留资涨 + SEO 渠道跳出率降 → ②落地页段提升（SEO 内容见效）。
**持平类定位**：看"哪段没动"——如全段指标平稳 + 内容更新频率为 0 → 全段持平（内容无更新）。

**这一步的产出**：变化段定位（6 段其一或全段），+ 初步归因方向。还没到根因。

### 6.4 第二步 · 下钻维度

定位到漏斗段后，在该段内下钻，找"是谁驱动/拖累的"。


| 变化段      | 下钻维度              | 调用的第二层数据                                               |
| -------- | ----------------- | ------------------------------------------------------ |
| ① 渠道到达   | 渠道                | `query_channel_detail`：各渠道 UV 明细                       |
| ② 落地页    | 渠道 + 页面           | `query_channel_detail` + `query_page_detail`：各渠道/页面跳出率 |
| ③ 站内浏览   | 页面路径 + 页面         | `query_page_detail`：页面跳转、停留、访问深度                       |
| ④ 转化入口触发 | 入口类型(CTA/客服) + 页面 | `query_page_detail` + 4 类行为埋点（点击行为）                    |
| ⑤ 转化交互   | 通道类型(表单/对话) + 页面  | 4 类行为埋点（交互行为 + 转化行为）                                   |
| ⑥ 成功留资   | 留资方式 + 线索质量       | 留资字段 + 线索质量评分                                          |


**下钻维度枚举（一期全做）**：

- 渠道（广告/社交/SEO/直接/外链）
- 页面（落地页/详情页/询盘页）
- 入口类型（CTA / 主动客服）
- 通道类型（表单 / 客服对话）
- 设备（移动/PC）
- 地域
- 时段

> 维度枚举固定，避免 Agent 乱钻。按数据可得性优先用渠道+页面+入口/通道类型，设备/地域/时段作为补充下钻维度。

**这一步的产出**：定位到"具体是哪个渠道/页面/入口/通道拖累"，比如"广告渠道落地页跳出率 52%，显著高于其他渠道"。还没到根因（为什么广告渠道跳出率高）。

### 6.5 第三步 · 假设生成 + 验证 + 区分真因

这是归因的核心，也是最难的一步。

#### 假设生成

基于第一步+第二步的定位，生成候选假设。假设来源两个：


| 来源                               | 例子                                  |
| -------------------------------- | ----------------------------------- |
| 知识库匹配（`lookup_cause_action_map`） | "广告渠道落地页跳出率高" → 命中知识库"落地页与广告创意不匹配"  |
| Agent 推理生成                       | 知识库没命中的新情况，Agent 基于数据 + 4 类行为埋点生成假设 |


**假设必须可验证**——每个假设要附带"如果是这个原因，应该还能观察到 XX 现象"。

**知识库没命中的新情况，Agent 自己生成假设要人工审核**，避免乱归因。审核通过后沉淀回知识库，下次同类直接命中。

#### 假设验证

对每个假设，调数据验证"应该还能观察到的现象"是否成立：

```text
假设：广告渠道落地页与创意不匹配
  → 验证现象1：广告渠道的落地页跳出率是否高于其他渠道？  → 查 query_channel_detail
  → 验证现象2：广告渠道的停留时长是否短于其他渠道？      → 查 query_page_detail
  → 验证现象3：广告渠道的滚动深度是否浅于其他渠道？      → 查 4 类行为埋点（页面浏览）
  → 验证现象4：是否近期改过广告创意但没改落地页？        → 查投放变更记录（如有）
```

**验证结果三档**：

- 现象全成立 → 假设升级为"高置信度根因"
- 部分成立 → "中置信度"，继续验证或并列
- 全不成立 → 排除该假设



#### 区分真因 vs 相关（最难点）


|     | 相关         | 真因                            |
| --- | ---------- | ----------------------------- |
| 定义  | A 和 B 同时变化 | A 导致 B                        |
| 判据  | 同时发生       | 时间先后（因在前果在后）+ 控制变量（固定其他看独立影响） |


**Agent 实操方法**（不追求严格因果推断，追求"可解释的合理归因"）：

1. **时间先后**：流量跌在前还是留资跌在前？流量先跌 → 流量是因；同时跌 → 可能共因。
2. **控制变量**：固定渠道看页面，固定页面看渠道。如果"只有广告渠道的落地页跳出率升，其他渠道没升"→ 不是全站落地页问题，是广告+落地页匹配问题。
3. **反证**：如果是这个原因，应该还有 XX 现象。查一下有没有。没有 → 降置信度。

**重要**：Agent 做的是"基于数据的合理归因"，不是严格因果证明。所以**每个根因必须带置信度**，不能给"确定就是它"的假象。

### 6.6 6 段漏斗标准原因库（流失 + 提升 + 持平，新增）

对齐架构设计文档 1.3.2 + 6 段漏斗修订 + 三类变化扩展，归因下钻的终点是这套标准原因库。Agent 生成的假设最终要落到对应类型的原因之一，避免乱归因。原因库分三类：

#### 一、流失原因库（对应异常类 · 为什么跌）


| 漏斗段      | 标准流失原因                         | 典型验证现象                             |
| -------- | ------------------------------ | ---------------------------------- |
| ① 渠道到达   | 流量不精准/渠道匹配差                    | 某渠道 UV 跌但跳出率升（来的不是目标人群）            |
| ② 落地页    | 内容不匹配搜索意图 / 加载慢 / 首屏价值弱        | 落地页跳出率高 + 滚动深度浅 + 停留短              |

**②落地页段「内容不匹配搜索意图」的三种细分**（接入意图数据后，归因可从模糊判断升级为精确结论，见 4.3.5）：

| 细分原因 | 含义 | 验证数据来源 |
| ------- | --- | ----------- |
| **搜索词-落地页错配** | 来路搜索词/广告关键词的意图，与落地页内容不符（如搜"价格"落到品牌页） | 搜索关键词 × 落地页对照 |
| **意图类型-内容供给错配** | 某类意图（价格咨询/对比/售后）的访客集中流失，因网站缺少该类内容供给 | 客服对话意图聚类 × 转化表现 |
| **内容缺口** | 站内搜索有明确需求但 0 结果/结果差——访客想要的内容网站根本没有 | 站内搜索词 0 结果率 |
| ③ 站内浏览   | 内容不吸引 / 站内导航差 / 没找到信息          | 平均页数低 + 站内搜索率高 + 出口集中在某页           |
| ④ 转化入口触发 | CTA 不明显 / 时机不对 / 没触发主动对话       | CTA 点击率低 + 滚动到 CTA 位置的比例低 + 客服发起率低 |
| ⑤ 转化交互   | 表单太复杂 / 客服回答不专业 / 信任感不足 / 等待焦虑 | 表单中途流失率高 + 客服对话负反馈多 + 提交前放弃率高      |
| ⑥ 成功留资   | 信任感不足 / 最后一步放弃 / 线索质量差         | 留资字段质量低 + 高价值线索占比降                 |




#### 二、提升原因库（对应提升类 · 为什么涨，用于夸赞）


| 漏斗段      | 标准提升原因                  | 典型验证现象                   |
| -------- | ----------------------- | ------------------------ |
| ① 渠道到达   | 流量变精准 / 渠道匹配变好          | 某渠道 UV 涨且跳出率降（来的更对口）     |
| ② 落地页    | 内容匹配搜索意图 / 加载变快 / 首屏价值强 | 落地页跳出率降 + 滚动深度深 + 停留长    |
| ③ 站内浏览   | 内容吸引 / 站内导航顺 / 找到信息     | 平均页数升 + 站内搜索率降 + 多页浏览占比升 |
| ④ 转化入口触发 | CTA 明显 / 时机对 / 主动对话触发好  | CTA 点击率升 + 客服发起率升        |
| ⑤ 转化交互   | 表单顺 / 客服专业 / 信任感足       | 表单完成率升 + 客服对话转化率升        |
| ⑥ 成功留资   | 信任感足 / 留资顺畅 / 线索质量好     | 留资率升 + 高价值线索占比升          |




#### 三、持平原因库（对应持平类 · 为什么没动，用于说明）


| 漏斗段  | 标准持平原因       | 典型验证现象                 |
| ---- | ------------ | ---------------------- |
| 全段通用 | 流量结构稳定但无新增渠道 | 各渠道 UV 占比不变，无新渠道进入     |
| 全段通用 | 内容无更新（长期未运营） | 内容更新频率为 0，页面无变化        |
| 全段通用 | 处于行业淡季（季节性）  | 同期行业整体下滑，本站持平已优于行业     |
| 全段通用 | 已到该站点转化天花板   | 转化率长期维持行业 P75 分位，无优化空间 |
| 全段通用 | 措施已执行但见效周期未到 | 上期措施执行中，T+7/T+30 复盘未到  |


**使用约束**：

- Agent 归因产出的根因必须能归到对应类型的原因库之一（或标注"新原因，待审核沉淀"）；
- 三套原因库是知识库 v1.0 的根因侧种子，与模块 4 的措施/夸赞/建议侧映射表配套；
- 新原因经人工审核后沉淀进库，越用越厚；
- **提升原因库的沉淀价值**：让"为什么涨"可复用——客户知道某次提升是"SEO 内容优化见效"，下次可继续强化；运营知道哪种措施真有效，沉淀为标准措施。



### 6.7 归因深度停止条件（防止 Agent 陷进去）

无限下钻是归因分析最大的坑。定四条停止规则：


| 停止条件       | 含义                                  |
| ---------- | ----------------------------------- |
| 已到可执行粒度    | 能给出具体措施了（如"优化广告渠道落地页"），不必再钻"为什么不匹配" |
| 数据不足       | 关键维度没数据，钻不下去                        |
| 下钻超 3 层仍无果 | 越钻越散，停                              |
| 置信度低于阈值    | 多假设并存且无法验证，停                        |


**默认下钻深度上限：3 层**（漏斗段 → 维度 → 假设验证）。超过 3 层强制停，进喊人流程。

### 6.8 喊人机制（什么时候不该 Agent 继续猜）


| 喊人场景              | 处理                         |
| ----------------- | -------------------------- |
| 多假设并存且无法用站内数据验证   | 输出"待人工确认"，列出候选假设 + 缺的数据    |
| 涉及外部因素（季节性、竞对、政策） | 站内数据归因不了，标注"疑似外部因素，建议人工判断" |
| 下钻超 3 层无果         | 停，输出已定位到的最深层 + 喊人          |
| 同一异常反复出现且上次方案无效   | 喊人，避免循环                    |


**喊人产出**：不是"我搞不定"，而是"已定位到 X 层，候选原因是 A/B，缺的数据是 C，建议人工介入判断"。给信息，不甩锅。

**喊人后处理**：**挂起任务等人工**，模块 4 不接喊人任务。人工确认/补充后，任务恢复流转进模块 4。

### 6.9 归因产出结构（交模块 4）

```text
AttributionResult {
  diagnosisTaskId,
  changeType: 'down' | 'up' | 'flat',                 // 变化类型（新增，继承模块2）
  funnelSegment: 'channel_arrival' | 'landing_page' | 'site_browsing' | 'conversion_entry' | 'conversion_interaction' | 'lead_success',
  drillDownDimensions: [...],
  rootCauses: [
    {
      cause: '广告渠道落地页与创意不匹配',
      causeCategory: '内容不匹配搜索意图',   // 归到 6.6 对应类型原因库（流失/提升/持平）
      confidence: 'high' | 'medium' | 'low',
      evidence: [...],
      isTrueCause: bool,
    }
  ],
  needsHumanReview: bool,
  humanReviewReason?: string,
  drillDepth: number,
  status: 'pending_solution'            // 待模块4按类型产出
}
```

**关键**：归因产出带 `changeType`（继承模块 2），决定模块 4 走哪条产出链：`down` → 措施+依据卡片；`up` → 夸赞（基于提升原因库）；`flat` → 说明+提升建议（基于持平原因库）。归因产出**多个根因并列**，带置信度排序，不强行收敛成一个。每个根因带 `causeCategory` 归到 6.6 对应类型原因库。

### 6.10 模块 3 边界声明

模块 3 只定义"为什么变化（三类都归因）"。**不定义**："什么算变化"（模块 2）、"怎么办/怎么夸"（模块 4）、"怎么执行"（模块 5）、"怎么复盘"（模块 6）。

模块 3 的交付物：第 6.3-6.9 节的归因框架（三类）+ 标准原因库（流失/提升/持平）+ 停止条件 + 喊人机制 + 产出结构。

---



## 7. 模块 4 · 应对产出



### 7.1 模块定位

模块 4 回答：**怎么办 / 怎么夸**。它接模块 3 的归因产出（带 `changeType`），**按变化类型产出不同内容**：


| changeType | 模块 4 产出                 | 去向        |
| ---------- | ----------------------- | --------- |
| `down`（异常） | 可执行措施清单（带依据卡片）          | 模块 5 调度执行 |
| `up`（提升）   | 夸赞文案（基于提升原因库，告诉客户哪里做对了） | 直接展示，不执行  |
| `flat`（持平） | 说明文案 + 提升建议（只出方案，不执行）   | 直接展示，不执行  |


```text
模块3 给根因（带changeType） → 模块4 按类型产出 → 异常进模块5执行 / 提升持平直接展示
```

**模块 4 只生成方案/夸赞/建议，不执行。** 执行边界（自动/确认/只出方案）是模块 5 的事，且只有异常类才进模块 5。

### 7.2 措施来源：双轨制


| 来源                                   | 机制                     | 适用场景                       |
| ------------------------------------ | ---------------------- | -------------------------- |
| **知识库匹配**（`lookup_cause_action_map`） | 根因标签 → 命中知识库标准措施       | 常见根因（如"落地页跳出率高"→"优化落地页内容"） |
| **Agent 生成**                         | 知识库没命中，Agent 基于根因推理提措施 | 新情况、复合根因                   |


**双轨衔接逻辑**：

```text
根因进来
  → 先查知识库
    → 命中 → 直接用标准措施（可解释、可复用）
    → 没命中 → Agent 生成新措施
      → 新措施要人工审核（延续模块3对"新假设要审核"的逻辑）
        → 审核通过 → 用，并沉淀回知识库
        → 审核不通过 → 退回 Agent 重新生成 or 喊人
```

**为什么新措施要人工审核**：Agent 生成的新措施没有历史验证，直接执行风险高。审核通过后沉淀回知识库，下次同类根因直接命中——与模块 2 模型兜底沉淀、模块 3 新假设沉淀，是同一套"越用越聪明"的逻辑。

### 7.3 措施结构（每个措施包含什么，含依据卡片）

每个措施是一个可被模块 5 执行的单元，必须带齐以下字段：


| 字段           | 说明                         | 例子                                                       |
| ------------ | -------------------------- | -------------------------------------------------------- |
| 措施描述         | 干什么                        | "优化广告渠道落地页内容，匹配广告创意"                                     |
| 对应根因         | 哪个根因催生的                    | "广告渠道落地页与创意不匹配"                                          |
| 根因置信度        | 继承模块3                      | 高/中/低                                                    |
| **依据卡片（新增）** | **三段式：现状数据 + 对比基准 + 具体动作** | "广告渠道落地页跳出率 52%（现状），高于行业平均 42%（对比基准），建议按搜索意图重写落地页内容（动作）" |
| 措施类型         | 止血 or 治本                   | 止血                                                       |
| 成本           | 低/中/高                      | 中                                                        |
| 见效周期         | 即时/天/周/月                   | 周                                                        |
| 风险           | 低/中/高                      | 低                                                        |
| 需调度的 L4 模块   | 对接哪个产品模块                   | AI 内容引擎                                                  |
| 建议执行边界       | 模块4建议，模块5最终定               | 确认后执行                                                    |


**依据卡片三段式**（对齐架构设计文档 1.3.2 + 老板会议"任务要有依据"）：

```text
依据卡片 = 现状数据 + 对比基准 + 具体动作
```

- **现状数据**：归因定位到的具体数值（如"广告渠道落地页跳出率 52%"）
- **对比基准**：行业平均 / 历史均值 / 其他渠道均值（如"高于行业平均 42%"）
- **具体动作**：可执行的操作（如"按搜索意图重写落地页内容"）

**为什么必须三段式**：老板会议明确"每个任务后面都要有为什么做这件事的依据"，但"不用把所有规则露出来"。依据卡片给客户看"为什么"，不露规则。缺对比基准，客户不知道"该不该急"；缺具体动作，客户不知道"做什么"。

> 注：措施**不带预期效果**字段。复盘口径由模块 6 基于"执行前 vs 执行后实际变化"判断，不基于"是否达到预期"。



### 7.4 优先级排序逻辑

多个根因 → 多个措施，按四个维度排序：


| 维度    | 权重方向 | 说明               |
| ----- | ---- | ---------------- |
| 根因置信度 | 高优先  | 高置信度根因的措施先排      |
| 影响面   | 大优先  | 影响指标多/影响流量大的根因先排 |
| 成本    | 低优先  | 成本低的措施先排（快速试）    |
| 见效周期  | 短优先  | 见效快的先排           |


**排序规则（定性，不搞复杂公式）**：

1. 先按**止血/治本**分两组，止血组排前（P0 异常优先止血）；
2. 组内按 **根因置信度 → 影响面 → 成本 → 见效周期** 顺序排；
3. 同等条件，低成本短周期优先（先摘低垂的果）。

**为什么不搞加权公式**：公式调参难、不可解释、老板问"为啥这个排第一"答不上来。定性排序可解释、可调。

### 7.5 止血 vs 治本

B2B 运营必须区分这两类，否则会"只治标"或"只治本走太慢"：


| 类型     | 特征      | 例子                 | 排序        |
| ------ | ------- | ------------------ | --------- |
| **止血** | 快、治标、应急 | 某页 404 暴增 → 先修 404 | 优先（尤其 P0） |
| **治本** | 慢、治根、长效 | 落地页内容质量优化          | 止血后       |


**规则**：

- P0 异常：至少配 1 个止血措施 + 1 个治本措施；
- P1/P2 异常：以治本为主，止血可选；
- 止血措施见效后，治本措施仍要排上（别止完血就不管了）。



### 7.6 一个根因可对应多个措施

**允许一个根因生成多个候选措施**，让客户/模块 5 选。不强行收敛成一个。

例子：根因"广告渠道落地页与创意不匹配" → 措施 A"优化落地页内容" + 措施 B"调整广告创意指向现有落地页"。两个都给，让客户选更合适的。

**但**：每个措施都要带完整的 7.3 字段 + 7.4 排序，不能给"裸措施"。

### 7.7 产出结构（按变化类型分三类，交模块 5 或直接展示）



#### 异常类 · 措施清单（交模块 5 执行）

```text
SolutionSet {
  attributionResultId,
  changeType: 'down',
  measures: [
    {
      measureId,
      description: '按搜索意图重写落地页内容',
      rootCause: '广告渠道落地页与创意不匹配',
      rootCauseConfidence: 'high',
      evidenceCard: {                    // 依据卡片
        currentValue: '广告渠道落地页跳出率 52%',
        benchmark: '高于行业平均 42%',
        action: '按搜索意图重写落地页内容',
      },
      measureType: 'quick_fix' | 'root_cure',
      cost: 'low' | 'medium' | 'high',
      timeToEffect: 'instant' | 'day' | 'week' | 'month',
      risk: 'low' | 'medium' | 'high',
      targetModule: 'ai_content_engine' | 'conversion_path_designer' | 'smart_form' | 'ai_cs_pro' | 'tool_agent' | 'none',
      suggestedBoundary: 'auto' | 'confirm' | 'advice_only',
      priority: number,
    }
  ],
  status: 'pending_execution'
}
```



#### 提升类 · 夸赞文案（直接展示，不进模块 5）

```text
PraiseResult {
  attributionResultId,
  changeType: 'up',
  changedMetric: '留资率',
  changeAmount: '3.2% → 3.7%（+0.5pp）',
  rootCauses: [                          // 为什么涨（基于提升原因库）
    {
      cause: 'SEO 内容优化见效',
      causeCategory: '内容匹配搜索意图',
      confidence: 'high',
      evidence: [...],
    }
  ],
  praiseText: '本周留资率提升 0.5pp，主要因 SEO 内容优化见效——落地页更匹配搜索意图，跳出率下降。继续保持内容更新节奏。',
  keepAdvice: '建议继续强化 SEO 内容产出，保持周更频率',
  status: 'done'                         // 提升类直接完成，不进模块5
}
```



#### 持平类 · 说明 + 提升建议（直接展示，建议只出方案不执行）

```text
FlatResult {
  attributionResultId,
  changeType: 'flat',
  changedMetric: '留资率',
  flatPeriods: 4,                         // 连续几期持平
  currentValue: '3.0%',
  rootCauses: [                           // 为什么没动（基于持平原因库）
    {
      cause: '内容无更新（长期未运营）',
      causeCategory: '内容无更新',
      confidence: 'high',
      evidence: [...],
    }
  ],
  explainText: '留资率连续 4 期维持 3.0%，主要因内容长期未更新，无新增流量与转化动力。',
  improveSuggestions: [                   // 提升建议（只出方案，不执行）
    {
      suggestion: '更新 TOP5 流量落地页内容',
      targetModule: 'ai_content_engine',
      expectedEffect: '预计提升留资率 0.3-0.5pp',
      boundary: 'advice_only',            // 持平类建议一律只出方案
    }
  ],
  status: 'done'                          // 持平类直接完成，不进模块5
}
```

**关键**：

- 异常类 `SolutionSet` 带 `evidenceCard`（依据卡片）和 `targetModule`（调度到的 L4 模块），`suggestedBoundary` 是模块 4 建议的执行边界，**最终执行边界由模块 5 定**；
- 提升类 `PraiseResult` 含 `praiseText`（夸赞文案）+ `keepAdvice`（保持建议），让客户知道哪里做对了、怎么保持；
- 持平类 `FlatResult` 含 `explainText`（说明）+ `improveSuggestions`（提升建议，一律 `advice_only` 只出方案不执行）；
- 模块 4 不越权决定"自动执行还是确认"（异常类才涉及，且由模块 5 拍板）。



### 7.8 根因→产出映射表（知识库 v1.0 种子，按 6 段漏斗 + 三类变化）

本节含三套映射表：**异常类→措施**（7.8.1，按 6 段漏斗）、**提升类→夸赞**（7.8.2）、**持平类→说明+建议**（7.8.3）。措施标注：类型（止血/治本）、成本、见效、风险、调度的 L4 模块、建议边界（喂给模块 5）。

#### 7.8.1 异常类 → 措施映射（按 6 段漏斗）



#### ① 渠道到达段异常（UV/PV 跌）


| 根因                 | 措施                              | 类型  | 成本  | 见效  | 风险  | 调度模块    | 建议边界     |
| ------------------ | ------------------------------- | --- | --- | --- | --- | ------- | -------- |
| 某渠道 UV 骤降·投放预算削减   | 建议恢复/调整预算                       | 治本  | 高   | 周   | 中   | 无（涉及钱）  | 只出方案     |
| 某渠道 UV 骤降·SEO 排名下降 | 优化 SEO（Title/Description/内容/内链） | 治本  | 中   | 月   | 低   | AI 内容引擎 | 确认后执行    |
| 某渠道 UV 骤降·SEO 被惩罚  | 排查违规项 + 申诉                      | 止血  | 中   | 月   | 高   | 无（人工排查） | 只出方案     |
| 全渠道 UV 下滑·季节性/节假日  | 标注外部因素，不执行                      | —   | —   | —   | —   | 无       | 只出方案（告知） |
| 外链 UV 跌·外链被删/降权    | 补充新外链                           | 治本  | 中   | 月   | 低   | 无（外链建设） | 只出方案     |
| SEO 流量跌·收录减少       | 提交 sitemap + 优化内容更新频率           | 治本  | 低   | 周   | 低   | AI 内容引擎 | 确认后执行    |




#### ② 落地页段异常（跳出率高）


| 根因                  | 措施              | 类型  | 成本  | 见效  | 风险  | 调度模块     | 建议边界  |
| ------------------- | --------------- | --- | --- | --- | --- | -------- | ----- |
| 落地页内容与流量来源不匹配       | 按来源意图重写落地页内容    | 治本  | 中   | 周   | 低   | AI 内容引擎  | 确认后执行 |
| 落地页加载慢              | 压缩图片/优化代码/上 CDN | 止血  | 中   | 即时  | 中   | 工具/Agent | 只出方案  |
| 落地页内容质量差（过短/占位/重复）  | 扩写/去占位/去重       | 治本  | 低   | 周   | 低   | AI 内容引擎  | 确认后执行 |
| 移动端体验差              | 移动端适配优化         | 治本  | 高   | 周   | 中   | 工具/Agent | 只出方案  |
| 落地页 404/死链          | 修复链接/301 重定向    | 止血  | 低   | 即时  | 低   | 工具/Agent | 自动执行  |
| 落地页 H1/Title 缺失或不规范 | 补充/修正 H1/Title  | 止血  | 低   | 即时  | 低   | AI 内容引擎  | 确认后执行 |




#### ③ 站内浏览段异常（平均页数/停留低）


| 根因              | 措施              | 类型  | 成本  | 见效  | 风险  | 调度模块    | 建议边界  |
| --------------- | --------------- | --- | --- | --- | --- | ------- | ----- |
| 站内导航差/内链缺失      | 优化导航结构 + 补内链    | 治本  | 中   | 周   | 低   | 转化路径设计器 | 确认后执行 |
| 内容不吸引（停留短/出口集中） | 优化承接页内容 + 加相关推荐 | 治本  | 中   | 周   | 低   | AI 内容引擎 | 确认后执行 |
| 站内搜索率高（没找到信息）   | 优化站内搜索 + 补缺失内容  | 治本  | 中   | 月   | 低   | AI 内容引擎 | 确认后执行 |




#### ④ 转化入口触发段异常（CTA 点击率/客服发起率低）


| 根因                    | 措施                 | 类型  | 成本  | 见效  | 风险  | 调度模块        | 建议边界  |
| --------------------- | ------------------ | --- | --- | --- | --- | ----------- | ----- |
| CTA 入口不足/不明显          | 增加 CTA + 优化位置      | 治本  | 中   | 周   | 低   | 转化路径设计器     | 确认后执行 |
| CTA 时机不对（滚到底才出现）      | CTA 位置从底部移至首屏+产品区  | 治本  | 低   | 周   | 低   | 转化路径设计器     | 确认后执行 |
| 没触发主动对话               | 配置智能客服主动对话策略       | 治本  | 中   | 周   | 低   | AI 智能客服 PRO | 确认后执行 |
| 按钮文案不诱人（"提交"vs"获取报价"） | 优化 CTA 文案 + A/B 测试 | 治本  | 低   | 周   | 低   | 转化路径设计器     | 确认后执行 |




#### ⑤ 转化交互段异常（表单完成率/客服转化率低）


| 根因                 | 措施                     | 类型  | 成本  | 见效  | 风险  | 调度模块        | 建议边界                |
| ------------------ | ---------------------- | --- | --- | --- | --- | ----------- | ------------------- |
| 询盘表单太长/太难填         | 精简字段（8→4）+ 进度条 + 分步填写  | 治本  | 中   | 周   | 中   | 智能表单系统      | 只出方案（一期）/ 确认后执行（二期） |
| 表单填写中途流失高          | 增加智能预填 + 减少必填项         | 治本  | 中   | 周   | 中   | 智能表单系统      | 只出方案（一期）            |
| 客服回答不专业/知识库不全      | 暴露知识库风险 + 引导补 FAQ/上传文档 | 治本  | 低   | 周   | 低   | AI 智能客服 PRO | 确认后执行               |
| 询盘路径断裂（详情页→询盘页链接断） | 修复链接                   | 止血  | 低   | 即时  | 低   | 工具/Agent    | 自动执行                |
| 信任感不足/等待焦虑         | 加信任徽章 + 即时响应提示         | 治本  | 低   | 周   | 低   | 转化路径设计器     | 确认后执行               |




#### ⑥ 成功留资段异常（留资率/留资字段质量低）


| 根因                | 措施                   | 类型  | 成本  | 见效  | 风险  | 调度模块    | 建议边界     |
| ----------------- | -------------------- | --- | --- | --- | --- | ------- | -------- |
| 产品详情页内容不符合行业规范    | 按行业规范重写详情页           | 治本  | 中   | 周   | 低   | AI 内容引擎 | 确认后执行    |
| 产品详情页缺规格表/售后等关键模块 | 补充缺失模块               | 治本  | 低   | 周   | 低   | AI 内容引擎 | 确认后执行    |
| 营销页内容陈旧未更新        | 更新营销页内容              | 治本  | 中   | 周   | 低   | AI 内容引擎 | 确认后执行    |
| 线索质量差（高价值线索占比降）   | 联动外贸客户洞察打标签 + 优化留资字段 | 治本  | 中   | 月   | 低   | 线索管理    | 只出方案     |
| 某页转化率持续走低且原因不明    | 建议人工排查 + A/B 测试      | —   | —   | —   | —   | 无       | 只出方案（喊人） |




#### 7.8.2 提升类 → 夸赞映射（基于提升原因库）

提升类不生成措施，只生成"夸赞文案 + 保持建议"。夸赞基于模块 3 归因到的提升原因，告诉客户哪里做对了、怎么保持。


| 提升原因（6.6 提升库）   | 夸赞要点                          | 保持建议               |
| --------------- | ----------------------------- | ------------------ |
| 流量变精准/渠道匹配变好    | "本周某渠道流量更精准，跳出率下降，来的客户更对口"    | 保持该渠道投放策略          |
| 内容匹配搜索意图/首屏价值强  | "落地页更匹配搜索意图，跳出率下降 Xpp，内容优化见效" | 保持内容更新节奏（周更）       |
| 内容吸引/站内导航顺      | "访客浏览深度提升，多页浏览占比升，内容吸引力增强"    | 继续优化站内导航与相关推荐      |
| CTA 明显/主动对话触发好  | "CTA 点击率提升 Xpp，转化入口触达更有效"     | 保持当前 CTA 位置与文案     |
| 表单顺/客服专业/信任感足   | "表单完成率/客服转化率提升，转化摩擦减少"        | 保持表单精简度与客服话术       |
| 信任感足/留资顺畅/线索质量好 | "留资率与线索质量双升，信任建设见效"           | 继续强化信任元素（案例/资质/保障） |


**夸赞文案生成原则**：

- 必须带**具体数据**（"跳出率下降 Xpp"），不空夸；
- 必须点明**做对了什么**（基于提升原因），让客户可复用；
- 附**保持建议**，告诉客户怎么维持这个好势头。



#### 7.8.3 持平类 → 说明 + 提升建议映射（基于持平原因库）

持平类生成"说明文案 + 提升建议（只出方案，不执行）"。说明基于持平原因，提升建议指向可优化的漏斗段。


| 持平原因（6.6 持平库） | 说明要点                           | 提升建议（只出方案）                   |
| ------------- | ------------------------------ | ---------------------------- |
| 流量结构稳定但无新增渠道  | "各渠道占比稳定，但无新增流量来源，增长缺乏新动力"     | 建议拓展新渠道（如社媒/外链）              |
| 内容无更新（长期未运营）  | "内容长期未更新，无新增流量与转化动力"           | 建议更新 TOP5 流量落地页内容（调 AI 内容引擎） |
| 行业淡季（季节性）     | "处于行业淡季，本站持平已优于行业整体下滑"         | 建议淡季做内容储备，旺季前上线              |
| 已到该站点转化天花板    | "转化率长期维持行业 P75 分位，已达当前结构天花板"   | 建议结构性优化（改版/新增转化通道）           |
| 措施已执行但见效周期未到  | "上期措施执行中，复盘周期未到，待 T+7/T+30 验效" | 等待复盘，暂不加新措施                  |


**持平类建议一律** `advice_only`：只出方案，不调度执行。客户认可后可手动触发（二期）或等下期定时归因时再判。

#### 7.8.4 三套映射表定位

1. **这是知识库 v1.0 的种子**，不是穷举。后续靠模块 2 模型兜底沉淀、模块 3 新假设沉淀、模块 4 新措施/夸赞/建议审核沉淀，越用越厚。
2. **异常类"调度模块"列覆盖 L4 全部 5 个产品模块**：AI 内容引擎、转化路径设计器、智能表单系统、AI 智能客服 PRO、工具/Agent 直调，外加"只出方案"（涉及钱/外部/线索管理）。一期智能表单未上线，表单类一期只出方案。
3. **异常类"建议边界"列就是模块 4 给模块 5 的建议**，模块 5 最终拍板。规律：止血 + 低成本 + 低风险 + 技术修复类 → 倾向自动；治本 + 涉及内容/钱/外部 → 倾向确认或只出方案。
4. **提升类夸赞 + 持平类建议不进模块 5**：提升类止于展示夸赞，持平类建议一律只出方案不执行。只有异常类才进模块 5 调度执行。



### 7.9 模块 4 边界声明

模块 4 只定义"怎么办/怎么夸（按类型生成措施/夸赞/建议 + 依据卡片）"。**不定义**："为什么变化"（模块 3）、"怎么执行、执行边界"（模块 5，且只接异常类）、"怎么复盘"（模块 6）。

模块 4 的交付物：第 7.2-7.8 节的措施来源 + 措施结构（含依据卡片）+ 排序逻辑 + 止血治本 + 三类产出结构 + 三套映射表（措施/夸赞/建议）。

---



## 8. 模块 5 · 执行



### 8.1 模块定位

模块 5 回答：**怎么执行、执行边界怎么划**。它**只接模块 4 的异常类措施清单**（`changeType: 'down'`，带建议边界 + 依据卡片 + 调度模块），决定每个措施最终走哪档，并调度 L4 对应产品模块执行。提升类夸赞、持平类建议**不进模块 5**（止于模块 4 展示）。

```text
模块4 给异常类措施（带建议边界+调度模块） → 模块5 定最终边界 + 调度L4模块执行 → 模块6 复盘
（提升类夸赞 / 持平类建议 不进模块5，直接展示）
```

**模块 5 不生成措施、不归因、不处理提升/持平，只管"异常类措施的执行边界 + 调度 + 状态"。**

### 8.2 三档边界定义


| 档         | 含义                       | 客户动作      |
| --------- | ------------------------ | --------- |
| **自动执行**  | Agent 直接调度 L4 模块/工具，不等客户 | 无需操作，事后通知 |
| **确认后执行** | Agent 准备好方案，等客户点"确认"才执行  | 必须点确认     |
| **只出方案**  | Agent 只给建议，不调度，客户自己去做    | 客户自行处理    |




### 8.3 边界判定规则（基于四个因素，不看异常级别）

模块 4 给的是"建议边界"，模块 5 最终拍板。判定依据：


| 因素   | 自动执行倾向                     | 确认后执行倾向     | 只出方案倾向           |
| ---- | -------------------------- | ----------- | ---------------- |
| 措施类型 | 技术止血（404/SSL/sitemap/链接修复） | 内容优化（重写/补充） | 涉及钱/外部（预算/外链/申诉） |
| 风险   | 低                          | 低-中         | 中-高              |
| 可逆性  | 可逆（能回滚）                    | 部分可逆        | 不可逆              |
| 异常级别 | **不限**（P0/P1/P2 均可自动）      | P0/P1/P2 均可 | P0/P1/P2 均可      |


**关键规则**：**P0 允许自动执行**。自动执行的判定只看"类型 + 风险 + 可逆性"，不看异常级别。技术止血类措施即使对应 P0 异常，也可自动执行——因为这类措施低风险、可逆、且止血刻不容缓。

### 8.4 对模块 4 映射表的最终确认

模块 4 映射表里 5 条建议"自动执行"的措施，**全部保留自动执行**，不因 P0 降级：


| 措施           | 最终边界 | 调度方式           |
| ------------ | ---- | -------------- |
| 落地页 404/死链修复 | 自动执行 | 调工具/Agent 修复链接 |
| SSL 续签       | 自动执行 | 调工具/Agent 续签   |
| 死链/4xx 修复    | 自动执行 | 调工具/Agent 修复   |
| Sitemap 生成   | 自动执行 | 调工具/Agent 生成   |
| 询盘路径断裂修复     | 自动执行 | 调工具/Agent 修复链接 |




### 8.5 执行流程

```text
措施清单进来（带模块4建议边界+调度模块）
  ↓
模块5 判定最终边界
  ├─ 自动执行 → 直接调度L4模块/工具 → 执行中 → 成功/失败 → 通知客户
  ├─ 确认后执行 → 推给客户确认 → 客户确认 → 调度L4模块 → 成功/失败
  │            → 客户拒绝/忽略 → 任务关闭（记原因）
  │            → 超时一周 → 自动关闭
  └─ 只出方案 → 展示方案（含依据卡片） → 客户自行处理 → 任务关闭（不追踪执行）
```

**确认后执行的等待机制**：措施进"待确认"队列，客户在工作台看到红点；客户确认 → 执行；客户拒绝 → 关闭并记原因；超时一周（复用模块 2 过期机制）→ 自动关闭。

### 8.6 调度机制（覆盖 L4 全部 5 个产品模块）

模块 5 根据措施的 `targetModule` 字段，调度 L4 对应产品模块。**调度已存在的产品模块，不新建执行能力**：


| targetModule               | 调度的 L4 模块       | 执行方式                           | 一期落地         |
| -------------------------- | --------------- | ------------------------------ | ------------ |
| `ai_content_engine`        | **AI 内容引擎**     | 调营销页/详情页生成子 Agent，按搜索意图重写/生成内容 | ✅            |
| `conversion_path_designer` | **转化路径设计器**     | CTA 位置移动、心理学触发器配置、A/B 测试变体生成   | ✅            |
| `smart_form`               | **智能表单系统**      | 字段精简、分步填写、进度条、动态字段             | ⏸ 二期（一期只出方案） |
| `ai_cs_pro`                | **AI 智能客服 PRO** | 暴露知识库风险，引导补 FAQ/上传文档；主动对话策略配置  | ✅            |
| `tool_agent`               | **工具/Agent 直调** | 404/SSL/sitemap/链接修复，技术止血类     | ✅            |
| `none`                     | **只出方案**        | 不调度，展示方案 + 依据卡片，客户自行           | ✅            |


**关键**：

- 一期智能表单系统未上线，`smart_form` 类措施一期只出方案，二期表单系统上线后转"确认后执行"。
- 技术修复类（`tool_agent`）通过调用工具或直接调用 Agent 方式执行，不走内容生成 skill。
- 调度时把模块 4 的"依据卡片"一并传给被调度模块，作为执行依据留存。



### 8.7 执行状态管理

每条措施执行后有状态流转：


| 状态                | 含义              |
| ----------------- | --------------- |
| `pending_confirm` | 待客户确认           |
| `executing`       | 执行中（L4 模块/工具在跑） |
| `success`         | 执行成功            |
| `failed`          | 执行失败（记错误原因）     |
| `rejected`        | 客户拒绝执行          |
| `expired`         | 超时未确认，自动关闭      |
| `advice_only`     | 只出方案，客户自行处理     |


**失败处理**：执行失败 → 记错误原因 → 措施回退到"待确认"（让客户决定要不要重试）→ **不自动重试** → 失败原因进模块 6 复盘。

### 8.8 执行产出（交模块 6 复盘）

```text
ExecutionResult {
  measureId,
  finalBoundary: 'auto' | 'confirm' | 'advice_only',
  targetModule: string,                // 实际调度的L4模块（advice_only则无）
  executedSkill?: string,
  status: 'success' | 'failed' | 'rejected' | 'expired' | 'advice_only',
  executedAt?: timestamp,
  errorReason?: string,
  rejectReason?: string,
  metricSnapshotBefore: {...},         // 执行前指标快照（模块6复盘用）
  salesSnapshotBefore?: {...},         // 执行前销售转化快照（新增，模块6追销售转化用）
  status: 'pending_review'
}
```

**关键**：执行产出带**执行前的指标快照 + 销售转化快照**。因为模块 4 不带预期效果，模块 6 复盘只能靠"执行前 vs 执行后实际变化"判断，所以执行前必须留快照。新增 `salesSnapshotBefore` 供模块 6 追踪销售转化贡献。

**只出方案类不追踪**：`advice_only` 类措施，模块 5/6 不追踪执行效果（客户自行处理，无法追踪）。

### 8.9 模块 5 边界声明

模块 5 只定义"怎么执行 + 边界 + 调度 + 状态"，且**只处理异常类措施**。**不定义**："为什么变化"（模块 3）、"怎么办/怎么夸"（模块 4）、"怎么复盘"（模块 6）；**不处理**提升类夸赞、持平类建议（止于模块 4）。

模块 5 的交付物：第 8.3-8.8 节的边界规则 + 执行流程 + 调度机制（覆盖 L4 全部 5 模块，只对异常类）+ 状态管理 + 产出结构。

---



## 9. 模块 6 · 复盘



### 9.1 模块定位

模块 6 回答：**执行后效果怎么样**。它接模块 5 的执行产出（带执行前指标快照 + 销售转化快照），对比执行前后变化，判定措施成效，并把经验沉淀回知识库。

```text
模块5 给执行结果（带执行前快照） → 模块6 复盘判定 + 沉淀 → 知识库越用越厚 → 下一轮归因更准
```

**模块 6 只复盘和沉淀，不执行、不归因、不生成措施。**

### 9.2 复盘周期（按措施见效周期分档）

不同措施见效周期不同（模块 4 标了即时/天/周/月），复盘不搞一刀切：


| 措施见效周期  | 复盘周期 | 适用                       |
| ------- | ---- | ------------------------ |
| 即时 / 天  | T+3  | 止血类（404 修复、SSL 续签、链接修复）  |
| 周       | T+7  | 治本类（内容优化、SEO 优化、CTA 优化）  |
| 月       | T+30 | 长效类（结构化数据补充、外链建设、线索质量优化） |
| 未知 / 兜底 | T+7  | 默认                       |


**复盘由定时触发**：到复盘周期时自动拉执行后数据对比。手动触发可选（客户想提前看效果）。

### 9.3 成功判定标准（追到销售转化贡献，新增）

因模块 4 不带预期效果，复盘只能靠"执行前快照 vs 执行后实际值"对比。判定分两层：

**第一层·业务指标判定**（原有）：


| 判定           | 标准                    | 含义            |
| ------------ | --------------------- | ------------- |
| **成功**       | 异常指标回归到正常区间（模块 2 阈值内） | 措施起效，问题解决     |
| **部分改善**     | 异常指标有改善但未回归正常区间       | 措施部分有效，需追加或调整 |
| **未改善 / 恶化** | 异常指标无变化或更差            | 措施无效或原因判断错了   |


**第二层·销售转化贡献判定**（新增，对齐架构图"销售转化贡献追踪"）：


| 判定             | 标准                     | 含义                |
| -------------- | ---------------------- | ----------------- |
| **销售贡献显著**     | 执行后该客户/该措施关联线索的销售转化率提升 | 措施不仅修复指标，还贡献了销售结果 |
| **销售贡献待观察**    | 留资恢复但销售转化尚需更长周期        | 留资回来了，销售转化滞后，继续观察 |
| **留资恢复但销售未跟进** | 留资恢复但销售转化率未提升          | 留资量回来了但质量/跟进有问题   |


**判定逻辑**：

- 拉执行前快照（模块 5 的 `metricSnapshotBefore` + `salesSnapshotBefore`）；
- 拉执行后当前值（调模块 1 第一层数据 tool + 销售转化数据）；
- 对比业务指标变化方向 + 幅度；
- 对照模块 2 的阈值规则判定是否回归正常；
- 追踪销售转化贡献（如有销售数据接入）。

**销售转化数据接入说明**：销售转化数据依赖 CRM/销售系统对接，一期可能无法完全接入。一期先做业务指标判定，销售转化贡献字段预留，二期接入后补判定。

### 9.4 控制变量（多措施归因）

多措施同时执行时，要尽量归因到"哪个措施起作用了"：


| 场景                 | 归因方式                      |
| ------------------ | ------------------------- |
| 单措施执行              | 直接对比，归因清晰                 |
| 多措施同时执行            | 按措施对应的指标维度分别看变化，尽量归因到单个措施 |
| 多措施共效（难拆）          | 标注"多措施共效"，不强行归因到单个        |
| 外部因素干扰（季节性/竞对/节假日） | 标注"疑似外部因素"，降置信度           |


**原则**：宁可标注"共效/外部因素"，不强行归因。假归因比不归因更有害——会误导知识库沉淀错误经验。

### 9.5 沉淀回知识库机制（`save_lesson_to_kb`）

复盘结论通过 `save_lesson_to_kb` 写回知识库，三种情况都沉淀：


| 复盘结果 | 沉淀方式                          | 作用             |
| ---- | ----------------------------- | -------------- |
| 成功   | 沉淀为"标准措施"（异常场景+根因+措施+成功+销售贡献） | 强化知识库，下次同类直接命中 |
| 失败   | 沉淀为"负样本"（异常场景+根因+措施+失败）       | 避坑，下次同类不再推荐此措施 |
| 部分改善 | 标注"部分有效"，措施降级或调整后沉淀           | 下次同类可调整使用      |


**沉淀内容**：

```text
Lesson {
  scenario: '广告渠道落地页跳出率52%，环比升10pp',
  funnelSegment: 'landing_page',           // 6段漏斗哪段（新增）
  rootCause: '落地页与广告创意不匹配',
  causeCategory: '内容不匹配搜索意图',       // 6.6标准原因库分类（新增）
  measure: '按来源意图重写落地页内容',
  targetModule: 'ai_content_engine',        // 调度的L4模块（新增）
  reviewResult: 'success' | 'partial' | 'failed',
  changeAmount: '跳出率52%→44%',
  salesContribution?: '销售转化率提升X%',    // 销售转化贡献（新增）
  nextAdvice: '直接使用' | '调整后使用' | '弃用',
}
```

**关键**：成功和失败都沉淀。只沉淀成功不沉淀失败，知识库会变成"只会开药不会避坑"的偏科生。沉淀带 6 段漏斗段 + 标准原因分类 + 调度模块 + 销售贡献，让下次归因更准。

### 9.6 复盘产出结构

```text
ReviewResult {
  executionResultId,
  reviewPeriod: 'T+3' | 'T+7' | 'T+30',
  metricSnapshotAfter: {...},
  salesSnapshotAfter?: {...},               // 执行后销售转化快照（新增）
  verdict: 'success' | 'partial' | 'failed',
  salesVerdict?: 'sales_up' | 'sales_pending' | 'sales_flat',  // 销售转化贡献判定（新增）
  changeAmount: string,
  salesChangeAmount?: string,               // 销售转化变化幅度（新增）
  attributionConfidence: 'high' | 'medium' | 'low',
  externalFactors?: string,
  lessonId?: string,
  nextAction: 'close_task' | 'add_measure' | 'reopen_attribution' | 'human_review',
  status: 'done'
}
```



### 9.7 复盘后异常任务处理


| 复盘结果     | 异常任务处理                    |
| -------- | ------------------------- |
| 成功       | 关闭异常任务                    |
| 部分改善     | 继续观察（等下一周期复盘）或追加措施（回模块 4） |
| 未改善 / 恶化 | 重开归因（回模块 3 重新归因）或喊人（人工介入） |




### 9.8 模块 6 边界声明

模块 6 只定义"复盘判定 + 沉淀"。**不定义**："什么算异常"（模块 2）、"为什么异常"（模块 3）、"怎么办"（模块 4）、"怎么执行"（模块 5）。

模块 6 的交付物：第 9.2-9.7 节的复盘周期 + 成功判定（含销售转化贡献）+ 控制变量 + 沉淀机制 + 产出结构 + 任务处理。

---



## 10. 端到端案例集（9 例·三类变化闭环演示）

> 9 例覆盖三类变化（异常/提升/持平）、6 段漏斗、模块 5 四档执行边界（含不执行）、模块 6 复盘判定、L4 全部 5 个产品模块调度、意图数据归因（案例 9）。异常例（1-6、9）含：变化 → 归因（6 段定位）→ 根因（标准原因分类）→ 方案（依据卡片）→ 执行（调度 L4 模块）→ 复盘（含销售转化贡献）；提升例（7）含：变化 → 归因 → 夸赞；持平例（8）含：变化 → 归因 → 说明 + 建议。



### 案例 1 · 落地页优化（成功 · 确认后执行）

②落地页段异常 → 调 AI 内容引擎 → T+7 成功


| 环节  | 内容                                                                                              |
| --- | ----------------------------------------------------------------------------------------------- |
| 异常  | 留资率 3.2%→2.7%（环比跌 0.5pp，P0），UV 未跌                                                               |
| 归因  | ①定位②落地页段（UV 没跌+跳出率升）②下钻渠道（广告渠道跳出率 52%，显著高于其他 42%）③假设"落地页与创意不匹配"→验证广告渠道落地页跳出率高于其他✓、停留时长短✓、滚动深度浅✓ |
| 根因  | 广告渠道落地页与创意不匹配（高置信度·标准原因：内容不匹配搜索意图）                                                              |
| 方案  | 依据卡片：广告渠道落地页跳出率 52%（现状），高于行业平均 42%（对比基准），按搜索意图重写落地页内容（动作）。治本·中成本·周·低风险·调 AI 内容引擎·确认后执行          |
| 执行  | 客户确认→调 AI 内容引擎（营销页生成子 Agent）→执行前快照跳出率 52%                                                       |
| 复盘  | T+7 跳出率 52%→44%（<45% 正常），同期其他渠道无变化→成功；销售转化贡献：该渠道线索销售转化率提升 8%→销售贡献显著。沉淀"直接使用"                    |




### 案例 2 · 404/死链修复（成功 · 自动执行）

②落地页段异常（技术因素）→ 调工具自动修复 → T+3 成功


| 环节  | 内容                                                                                    |
| --- | ------------------------------------------------------------------------------------- |
| 异常  | 健康度问题列表新增 P0"死链暴增 23 条"（待处理问题数↑，P2），承接段跳出率微升                                          |
| 归因  | ①定位②落地页段（技术因素捣乱）②下钻页面（死链集中在落地页/产品页）③假设"死链导致跳出"→验证死链页跳出率显著高✓                           |
| 根因  | 落地页 404/死链（高置信度·标准原因：加载慢/技术问题）                                                        |
| 方案  | 依据卡片：死链 23 条集中在落地页（现状），健康度问题列表 P0（对比基准），修复链接/301 重定向（动作）。止血·低成本·即时·低风险·调工具/Agent·自动执行 |
| 执行  | 自动调工具修复→执行前快照死链 23 条                                                                  |
| 复盘  | T+3 死链 23→0，跳出率回落→成功；销售贡献待观察（短期）。沉淀"直接使用"                                             |




### 案例 3 · 预算削减（只出方案 · 不追踪）

①渠道到达段异常 → 疑似外部因素 → 只出方案


| 环节  | 内容                                                                                  |
| --- | ----------------------------------------------------------------------------------- |
| 异常  | 广告渠道 UV 494→296（环比跌 40%，P1），留资数同步跌                                                  |
| 归因  | ①定位①渠道到达段（UV 跌+留资跌）②下钻渠道（仅广告渠道骤降，其他正常）③假设"预算削减"→站内无变更排除站内因素，疑似外部因素无法站内验证→标注"疑似外部因素" |
| 根因  | 广告投放预算削减（中置信度·外部因素）                                                                 |
| 方案  | 依据卡片：广告渠道 UV 494→296 跌 40%（现状），其他渠道正常（对比基准），建议恢复/调整预算（动作）。治本·高成本·周·中风险·无调度模块·只出方案   |
| 执行  | 只展示方案 + 依据卡片，不调度，不追踪                                                                |
| 复盘  | 无（只出方案类设计上不追踪）                                                                      |




### 案例 4 · SEO 收录减少（未改善 · 失败沉淀）

①渠道到达段异常 → 调 AI 内容引擎补结构化数据 → T+30 未改善


| 环节  | 内容                                                                                                             |
| --- | -------------------------------------------------------------------------------------------------------------- |
| 异常  | 外贸 SEO 渠道 UV 222→188（环比跌 15%，P1），留资率同步跌                                                                        |
| 归因  | ①定位①渠道到达段②下钻渠道（仅 SEO 降）③假设"收录减少"→验证 SEO 排名未降✓排除排名、Search Console 收录数下降✓                                        |
| 根因  | SEO 收录减少（高置信度·标准原因：流量不精准/渠道匹配差）                                                                                |
| 方案  | 依据卡片：SEO 渠道 UV 222→188 跌 15%（现状），排名未降但收录数下降（对比基准），补结构化数据 JSON-LD + 提交 sitemap（动作）。治本·低成本·月·低风险·调 AI 内容引擎·确认后执行 |
| 执行  | 客户确认→调 AI 内容引擎补结构化数据→执行前快照 SEO UV 222                                                                          |
| 复盘  | T+30 SEO UV 222→218（无变化）→未改善；销售贡献待观察。沉淀"弃用，需回模块 3 重新归因"，异常任务重开归因                                               |




### 案例 5 · CTA 文案优化（部分改善）

④转化入口触发段异常 → 调转化路径设计器 → T+7 部分改善


| 环节  | 内容                                                                                          |
| --- | ------------------------------------------------------------------------------------------- |
| 异常  | 留资率 2.7%（环比跌 0.5pp，P0），UV 正常、跳出率正常→④转化入口触发段                                                 |
| 归因  | ①定位④转化入口触发段（UV/跳出正常，留资跌）②下钻入口类型（CTA 点击率仅 15%，低于行业 25%）③假设"CTA 文案不诱人"→验证 A/B"获取报价"点击率高于"提交"✓ |
| 根因  | CTA 按钮文案不诱人（中置信度·标准原因：CTA 不明显/时机不对）                                                         |
| 方案  | 依据卡片：CTA 点击率 15%（现状），低于行业平均 25%（对比基准），优化 CTA 文案 + A/B 测试（动作）。治本·低成本·周·低风险·调转化路径设计器·确认后执行    |
| 执行  | 客户确认→调转化路径设计器生成文案变体→执行前快照留资率 2.7%                                                           |
| 复盘  | T+7 留资率 2.7%→3.0%（有改善但未回正常≥3.2%）→部分改善；销售贡献待观察。沉淀"调整后使用（可叠加增加 CTA 入口）"，追加措施回模块 4             |




### 案例 6 · 产品详情页优化（成功 · 确认后执行）

⑥成功留资段异常（页面转化）→ 调 AI 内容引擎 → T+7 成功


| 环节  | 内容                                                                                                     |
| --- | ------------------------------------------------------------------------------------------------------ |
| 异常  | 页面转化率跌幅 TOP5 入榜——某产品详情页 /products/cnc-machining 转化率 3.8%→1.2%（环比跌 2.6pp，入榜即异常信号，P2）                    |
| 归因  | ①定位⑥成功留资段（该页 UV 正常、跳出率略升、留资率跌）②下钻页面（定位到具体该详情页）③假设"详情页内容质量差"→验证：该页停留 32s（短）✓、内容字数低于行业均值✓、缺规格表/售后模块✓     |
| 根因  | 产品详情页内容不符合行业规范+缺关键模块（高置信度·标准原因：内容不吸引/没找到信息）                                                            |
| 方案  | 依据卡片：该详情页转化率 3.8%→1.2%（现状），停留 32s 低于行业 60s（对比基准），按行业规范重写详情页+补规格表/售后模块（动作）。治本·中成本·周·低风险·调 AI 内容引擎·确认后执行 |
| 执行  | 客户确认→调 AI 内容引擎（产品详情页生成子 Agent）重写→执行前快照该页转化率 1.2%                                                       |
| 复盘  | T+7 该页转化率 1.2%→3.5%（回升到正常区间）→成功；销售贡献：该页来源线索销售转化率提升 12%→销售贡献显著。沉淀"直接使用（按行业规范重写+补规格表为标准措施）"              |




### 案例 7 · SEO 内容优化见效（提升 · 夸赞不执行）

②落地页段提升 → 归因夸赞 → 直接展示，不执行


| 环节  | 内容                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------- |
| 变化  | 留资率 3.2%→3.7%（环比涨 0.5pp，触发提升规则），UV 同步涨 8%                                                                   |
| 归因  | ①定位②落地页段（UV 涨+跳出率降）②下钻渠道（SEO 渠道跳出率从 48% 降到 41%，其他渠道无明显变化）③假设"SEO 内容优化见效"→验证：SEO 落地页滚动深度变深✓、停留时长变长✓、近期有周更内容✓ |
| 根因  | SEO 内容优化使落地页更匹配搜索意图（高置信度·标准提升原因：内容匹配搜索意图/首屏价值强）                                                             |
| 产出  | 夸赞文案："本周留资率提升 0.5pp，主要因 SEO 内容优化见效——落地页更匹配搜索意图，SEO 渠道跳出率从 48% 降到 41%。继续保持内容周更节奏。" 保持建议：继续强化 SEO 内容产出，保持周更频率 |
| 执行  | 无（提升类不进模块 5，直接展示绿色夸赞卡）                                                                                      |
| 复盘  | 无（提升类不执行，无需复盘；归因结论沉淀为"提升原因+保持建议"供下次复用）                                                                      |




### 案例 8 · 内容长期未更新（持平 · 说明+建议只出方案）

全段持平 → 归因说明 + 提升建议（只出方案）→ 直接展示，不执行


| 环节  | 内容                                                                                                          |
| --- | ----------------------------------------------------------------------------------------------------------- |
| 变化  | 留资率连续 4 期维持 3.0%±0.1pp（触发持平规则），UV/PV/跳出率均无明显变化                                                              |
| 归因  | ①定位全段（各段指标均平稳）②下钻渠道/页面（各渠道占比稳定，无新增渠道；TOP5 落地页内容更新频率为 0）③假设"内容无更新（长期未运营）"→验证：内容更新记录近 30 天为 0✓、页面无变化✓、无新内容上线✓ |
| 根因  | 内容无更新（长期未运营）（高置信度·标准持平原因：内容无更新）                                                                             |
| 产出  | 说明文案："留资率连续 4 期维持 3.0%，主要因内容长期未更新，无新增流量与转化动力。" 提升建议：更新 TOP5 流量落地页内容（调 AI 内容引擎，预计提升留资率 0.3-0.5pp，只出方案不执行）    |
| 执行  | 无（持平类建议一律 advice_only，不进模块 5；客户认可后可等下期定时归因再判，或二期手动触发）                                                       |
| 复盘  | 无（持平类不执行，无需复盘；若客户采纳建议手动优化，下期归因会判为提升/异常，再走对应流程）                                                              |




### 案例 9 · 意图内容缺口（成功 · 确认后执行 · 智能营销页生成）

②落地页段异常（意图维度）→ 调 AI 内容引擎 · 智能营销页生成 Agent → T+14 成功

| 环节  | 内容 |
| --- | --- |
| 异常  | ②落地页段跳出率 46%→53%（环比升 7pp，P1），UV 未跌；同期站内搜索量周环比 +60%，两个信号联动异常（模型兜底命中，人工标注为真异常） |
| 归因  | ①定位②落地页段（UV 没跌+跳出率升）②下钻意图数据（调 `query_search_intent`）：站内搜索词中"CNC 加工配件/主轴配件"类占比升至 22%，但 0 结果率 85%、搜索后跳出率 90%；SEO 来路关键词中配件类词点击量上升，落地页却都是通用产品列表页 ③假设"内容缺口——配件采购意图无承接内容"→验证：配件类搜索词无匹配页面✓、客服对话中配件咨询占比升至 18%✓、竞品均有配件专区✓ |
| 根因  | 配件类采购意图存在内容缺口（高置信度·标准原因：内容不匹配搜索意图 → 细分：内容缺口） |
| 方案  | 依据卡片：配件类站内搜索周环比 +60%、0 结果率 85%（现状），竞品均有配件专区承接该意图（对比基准），生成"CNC 加工配件"智能营销页承接该意图（动作）。治本·中成本·周·低风险·调 AI 内容引擎（智能营销页生成 Agent）·确认后执行 |
| 执行  | 客户确认 → 调 AI 内容引擎的**智能营销页生成 Agent**：输入意图数据包（配件类搜索词聚类 + 客服对话高频问题 + 产品库配件参数）→ 生成配件专题营销页（配件选型表/适配机型/FAQ）→ 客户审核上线 → 执行前快照：配件搜索 0 结果率 85%、落地页跳出率 53% |
| 复盘  | T+14：配件类搜索 0 结果率 85%→0%，配件意图访客跳出率 53%→38%，新页带来留资 12 条 → 成功；销售转化贡献：配件页来源线索 2 条进入报价流程 → 销售贡献显著。沉淀"直接使用"：意图缺口 → 智能营销页生成 为标准措施；**配件类搜索词聚类结果回流 L3 搜索意图精准匹配，更新意图库** |



### 案例集覆盖说明


| 维度        | 覆盖案例                                                                        |
| --------- | --------------------------------------------------------------------------- |
| 三类变化      | 异常（1-6/9）、提升（7）、持平（8）—三类全覆盖                                                   |
| 6 段漏斗     | ①渠道到达（3/4）、②落地页（1/2/7/9）、④转化入口触发（5）、⑥成功留资（6）、全段持平（8）—5 段覆盖（③站内浏览/⑤转化交互待补）     |
| 模块 5 执行边界 | 自动执行（2）、确认后执行（1/4/5/6/9）、只出方案（3）、不执行（7/8）—四档全覆盖                               |
| 模块 6 复盘判定 | 成功（1/2/6/9）、未改善（4）、部分改善（5）、不复盘（7/8）—全覆盖                                       |
| 调度 L4 模块  | AI 内容引擎（1/4/6/8建议/9）、工具/Agent（2）、转化路径设计器（5）、只出方案（3）、不调度（7）—全覆盖（智能表单/智能客服待补案例） |
| 销售转化贡献    | 销售贡献显著（1/6/9）、销售贡献待观察（2/4/5）、不追踪（3）、不适用（7/8）—全覆盖                              |
| 意图数据归因    | 搜索词-落地页错配/内容缺口（9）—意图数据（4.3.5）端到端演示：意图下钻 → 智能营销页生成 Agent → 复盘回流 L3 意图库 |


---



## 11. 落地计划与分期



### 11.1 一期范围（跑通闭环）


| 模块          | 一期内容                                                          |
| ----------- | ------------------------------------------------------------- |
| 模块 1 指标体系   | 第一层 6 类指标 + 第二层渠道/页面明细 + 4 类行为埋点接口 + 6 段漏斗留存 + 用户意图数据    |
| 模块 2 指标变化发现 | 规则层（含正向/持平）+ 模型兜底 + **定时触发（周期可配 7 天+，站点设置配置）** + 三类变化判定       |
| 模块 3 归因分析   | 6 段漏斗定位 + 5 维度下钻 + **三类变化都归因** + 标准原因库（流失/提升/持平）+ 假设验证 + 喊人   |
| 模块 4 应对产出   | **按类型产出**：异常→措施+依据卡片 / 提升→夸赞 / 持平→说明+建议（只出方案）+ 三套映射表 + 排序     |
| 模块 5 执行     | **只接异常类**：三档边界 + 调度 AI 内容引擎/转化路径设计器/智能客服 PRO/工具（4 类，智能表单只出方案） |
| 模块 6 复盘     | 复盘周期 + 业务指标判定（销售转化字段预留，二期接入）+ 沉淀（三类归因都沉淀）                     |
| 站点设置        | 新增"归因分析周期"配置项（7/14/30 天或自定义≥7）                                |


**一期目标**：跑通"定时看数 → 判三类变化 → 归因（6 段漏斗）→ 按类型产出（措施/夸赞/建议）→ 异常类执行（调度 L4 模块）→ 复盘 → 沉淀"闭环，让客户既看到问题也看到成绩。

### 11.2 二期范围（补全能力）


| 项             | 二期内容                         |
| ------------- | ---------------------------- |
| 智能表单系统        | 上线后，表单类措施从"只出方案"转"确认后执行"     |
| 销售转化贡献        | 接入 CRM/销售数据，补全模块 6 销售转化判定    |
| ③站内浏览/⑤转化交互案例 | 补这两个漏斗段的端到端案例                |
| 智能客服/智能表单调度案例 | 补这两类调度的端到端案例                 |
| 持平类建议手动触发     | 客户采纳持平类建议后，可手动触发优化（仍走定时归因验效） |


> 注：触发方式一期已定为"只定时"，二期不再补实时/事件触发（场景 3-5 已废弃，见第 2.2 节理由）。二期聚焦补全执行能力与销售转化追踪。



### 11.3 时间节点（对齐老板会议）

- 今明两天：细化方案，明天跟研发（李莹）过方案；
- 下周：启动研发（祁阳 8 月初休假，8 月 4 号预产期，需在其休假前进入研发阶段）；
- 8 月：重点诊断 + 数据落盘，先搞 5 个客户网站验证产品详情页优化效果。

---



## 附录 A · 落地架构（方案 A · 单 skill 渐进版）

> 本附录定义模块 1 指标体系落地为 skill + tool 的最小可行架构。采用"单 skill 包办"的渐进方案，跑通后再拆为编排层 + 子 skill（方案 B，见附录 B）。



### A.1 架构总览

```text
growth_diagnosis（单 skill，承载模块 1-6 全链路）
  │
  ├─ 数据 tool（拉数，对应模块 1 第一层/第二层）
  │    ├─ query_business_metrics      北极星+流量段+承接段 + 30天日序列
  │    ├─ query_channel_metrics       各渠道 UV + 留资率 + 日序列
  │    ├─ query_page_conversion_rank  页面转化率最差TOP5 + 跌幅TOP5（带UV过滤）
  │    ├─ query_health_status         健康度分 + 待处理问题列表（对接线A）
  │    ├─ query_funnel_dropoff        6段漏斗各段留存/流失率（按需·第二层）
  │    ├─ query_channel_detail        渠道4指标明细（按需·第二层）
  │    ├─ query_page_detail           页面全排行/停留/访问深度（按需·第二层）
  │    ├─ query_behavior_events       4类行为埋点（按需·第二层，新增）
  │    └─ query_search_intent         搜索关键词+站内搜索词+客服对话意图（按需·第二层，新增，对接L3意图能力）
  │
  ├─ 知识库 tool（归因匹配 + 复盘沉淀）
  │    ├─ lookup_cause_action_map     原因→产出映射查询（模块4用，按6段漏斗+三类组织）
  │    └─ save_lesson_to_kb           复盘结论写回知识库（模块6用，带6段+原因分类+销售贡献）
  │
  ├─ 推理（放 skill 内，不做 tool）
  │    ├─ 变化判定（模块2·异常/提升/持平三类）
  │    ├─ 归因下钻（模块3·三类都归因·6段漏斗定位）
  │    └─ 应对产出（模块4·按类型：措施/夸赞/建议·含依据卡片生成）
  │
  └─ 调度 L4 产品模块（模块5，复用不新建）
       └─ ai_content_engine / conversion_path_designer / smart_form(二期) / ai_cs_pro / tool_agent
```



### A.2 Skill 定义 · growth_diagnosis


| 项       | 内容                                                                                                                               |
| ------- | -------------------------------------------------------------------------------------------------------------------------------- |
| skill 名 | `growth_diagnosis`                                                                                                               |
| 类型      | 编排型（承载分析全链路，非执行型）                                                                                                                |
| 触发      | **定时（周期可配 7 天+，站点设置配置）**；不设手动/实时/事件触发                                                                                            |
| 输入      | 站点 ID、周期（analysisPeriodDays）                                                                                                |
| 职责      | 拉第一层数据 → 判三类变化（异常/提升/持平）→ 按需拉第二层（含 4 类行为 + 6 段漏斗 + 意图数据）→ 对三类都归因（6 段定位）→ 按类型产出（异常措施+依据卡片 / 提升夸赞 / 持平说明+建议）→ 异常类调度 L4 模块执行 → 触发复盘 |
| 产出      | 一份"指标变化报告"进智能体任务队列，含三类变化条目（异常带措施+依据卡片+调度模块、提升带夸赞、持平带说明+建议）                                                                       |
| 边界      | 不自己改内容/发产品；执行动作交 L4 产品模块；提升/持平不执行                                                                                                |




### A.3 Tool 清单

**数据类 tool（9 个，前 4 个第一层必给，后 5 个第二层按需）**：


| Tool                          | 作用                     | 对应模块        |
| ----------------------------- | ---------------------- | ----------- |
| T1 query_business_metrics     | 北极星+流量段+承接段，带 30 天日序列  | 模块1 第一层 1-3 |
| T2 query_channel_metrics      | 各渠道 UV+留资率，带日序列        | 模块1 第一层 4   |
| T3 query_page_conversion_rank | 页面最差/跌幅 TOP5，带 UV 过滤   | 模块1 第一层 6   |
| T4 query_health_status        | 健康度分+问题列表（对接线A）        | 模块1 第一层 5   |
| T5 query_funnel_dropoff       | 6 段漏斗各段留存/流失率（按需）      | 模块1 第二层     |
| T6 query_channel_detail       | 渠道 4 指标明细（按需）          | 模块1 第二层     |
| T7 query_page_detail          | 页面全排行+停留+访问深度（按需）      | 模块1 第二层     |
| T8 query_behavior_events      | 4 类行为埋点（按需，新增）         | 模块1 第二层     |
| T9 query_search_intent        | 搜索关键词+站内搜索词+客服对话意图（按需，新增，对接 L3 意图能力） | 模块1 第二层 |


**知识库类 tool**：


| Tool                        | 作用                         | 对应模块 |
| --------------------------- | -------------------------- | ---- |
| T10 lookup_cause_action_map | 原因→措施映射查询（按 6 段漏斗组织）       | 模块4  |
| T11 save_lesson_to_kb       | 复盘结论写回知识库（带 6 段+原因分类+销售贡献） | 模块6  |


**不做成 tool 的部分**：变化判定、归因下钻、应对产出（含依据卡片/夸赞/建议）放 skill 逻辑；调度 L4 模块是 skill 间调度，非 tool。

### A.4 落地路径


| 阶段        | 内容                                                  | 产出                                         |
| --------- | --------------------------------------------------- | ------------------------------------------ |
| 第一期（方案 A） | 建 growth_diagnosis 单 skill + T1-T11 tool（9 数据 + 2 知识库）+ 复用 L4 模块 | 跑通"看数→归因（6段）→方案（依据卡片）→执行（调度L4）→复盘→进任务队列"闭环 |
| 拆分信号      | skill prompt 膨胀到顾此失彼 / 归因质量上不去 / 单 skill 耗时过长       | 触发方案 B 评估                                  |
| 第二期（方案 B） | 拆为 growth_orchestrator（Plan 层）+ 子 skill             | 见附录 B                                      |




### A.5 与现有架构的关系

- 本方案**不改 L4 现有产品模块**（AI 内容引擎/智能客服 PRO/转化路径设计器），只复用调度；智能表单系统二期上线后接入。
- 本方案**不改线 A 的 6 维度体检流程**，只通过 `query_health_status` 读其结果。
- 本方案**不依赖 Plan+Worker 多 Agent 架构**，单 Agent 即可跑通（方案 A）；方案 B 才需要架构升级。

---



## 附录 B · 目标架构（方案 B · 编排层 + 子 skill，待启用）

> 仅作预告，不在本期落地。当方案 A 出现拆分信号时启用。

```text
growth_orchestrator（Plan/编排层）
  ├─ metric_change_detect       模块2 指标变化发现（异常/提升/持平三类）
  ├─ metric_attribution        模块3 归因分析（三类都归因·6段漏斗）
  ├─ response_generation       模块4 应对产出（异常措施/提升夸赞/持平建议·含依据卡片）
  ├─ review_agent              模块6 复盘（含销售转化）
  └─ 调度 L4 产品模块            模块5 执行（只接异常类）
```

拆分后每个子 skill 职责单一、可独立优化与评估；tool 清单不变，由各子 skill 按需调用。

---



## 修订记录


| 版本       | 日期             | 说明                                                                                                                                                                             |
| -------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| v0.1     | 2026-07-29     | 首版：模块 1 指标体系定稿；模块 2-6 预告                                                                                                                                                       |
| v0.1.1   | 2026-07-30     | 增补附录 A（方案 A 落地架构）；附录 D 预告方案 B                                                                                                                                                  |
| v0.2     | 2026-07-30     | 模块 2 异常发现定稿                                                                                                                                                                    |
| v0.3     | 2026-07-30     | 模块 3 归因分析定稿                                                                                                                                                                    |
| v0.4     | 2026-07-30     | 模块 4 解决方案定稿 + 6.9 映射表                                                                                                                                                          |
| v0.5     | 2026-07-30     | 增补 8.11 端到端案例集（6 例）                                                                                                                                                            |
| **v1.0** | **2026-07-30** | **重构版：对齐数字门户 V2.0 产品架构。新增第 1-3 章（定位/用户场景/架构数据流）；6 段漏斗贯穿模块 3-4；措施加依据卡片三段式；调度链覆盖 L4 全部 5 模块；复盘追到销售转化贡献；案例集独立成章；编号重排修复断裂**                                                        |
| **v1.1** | **2026-07-30** | **三类变化扩展：模块 2「异常发现」→「指标变化发现」，判定异常/提升/持平三类；触发简化为定时（周期可配 7 天+，站点设置配置），废弃手动/实时/事件触发；模块 3 对三类都归因，补提升/持平原因库；模块 4 按类型产出（异常→措施 / 提升→夸赞 / 持平→说明+建议）；模块 5 只接异常类执行；案例集补至 8 例（含提升/持平案例）** |
| **v1.2** | **2026-07-30** | **意图数据扩展：对齐 L3「搜索意图精准匹配」。模块 1 新增 4.3.5 用户意图数据（搜索关键词/站内搜索词/客服对话意图）+ 4.5 要求 7；模块 3 流失原因库「内容不匹配搜索意图」细化为三种（搜索词-落地页错配/意图类型-内容供给错配/内容缺口）；案例集补案例 9（意图内容缺口 → 调 AI 内容引擎·智能营销页生成 Agent → 复盘回流 L3 意图库），9 例；附录 A 补 T12 query_search_intent** |
| **v1.3** | **2026-07-30** | **去掉客户分层：2.1 改为统一服务方式（所有客户同一套能力）；删除 4.3.4 客户分层与行业基准，意图数据顺延为 4.3.4；6 段漏斗留存率段间改用全称；附录 A 去掉 query_benchmark / customerTier** |
| **v1.4** | **2026-07-30** | **3.3 六段漏斗表「关键数据」拆为「第一层信号 / 第二层下钻」两列，与模块 1 三层数据视野同名同口径对齐（对齐工作方法论 v1.3）** |


