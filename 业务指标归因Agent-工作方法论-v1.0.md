# 业务指标归因 Agent 工作方法论

> **副标题**：AI 如何从 6 段业务指标出发，走完「体检 → 定位 → 下钻 → 归因 → 方案 → 执行 → 复盘」的完整闭环
>
> **版本**：v1.0 · 2026-07-30
> **配套文档**：《营销增长工作台-业务指标归因Agent-产品方案》v1.1（本文档与其模块 1-6 严格对齐，不引入新概念）

---

## 0. 这份文档讲什么

产品方案讲"产品长什么样"，本文档讲"**AI 到底怎么干活**"。

当老板问"这个归因 Agent 到底是怎么分析出原因的"，本文档就是答案：它把 Agent 的工作拆成**七个步骤**，每一步写清楚五件事——

| 每一步讲清 | 说明 |
| --- | --- |
| **输入** | 这一步拿到什么数据/信号 |
| **动作** | Agent 按什么顺序做什么 |
| **判断逻辑** | 关键决策点，依据什么规则/框架 |
| **输出** | 这一步产出什么，交给谁 |
| **护栏** | 防止 Agent 干错/干过界的机制 |

**方法论的一句话版本**：

> 以 **6 段转化漏斗**为骨架，以**三层数据视野**为输入，以**假设-验证**为归因内核，以**置信度**为诚实底线，以**三套原因库 + 知识库沉淀**为记忆，以**三档执行边界**为分寸，让 AI 像一个有经验、有分寸、会复盘的运营专家一样工作。

---

## 1. 一页总览：Agent 的七步工作流

### 1.1 分析骨架：6 段转化漏斗

**这套方法论的一切都挂在 6 段转化漏斗上**——指标挂在哪段，问题就定位到哪段，原因就归到哪段，措施就调度到对应的模块。先把骨架亮出来：

```text
① 渠道到达 → ② 落地页 → ③ 站内浏览 → ④ 转化入口触发 → ⑤ 转化交互 → ⑥ 成功留资
  (来没来)    (接住没)    (深看没)      (行动没)         (完成没)       (留成没)
```

| 段 | 名称 | 定义 | 第一层信号（体检必看） | 第二层下钻（按需调取，对齐第 3 章数据食谱） | 异常时归因方向 |
| --- | --- | --- | --- | --- | --- |
| ① | 渠道到达 | 访客从各渠道（广告/SEO/社媒/直接/外链）进入网站 | UV、PV；各渠道 UV | **渠道/页面明细**：各渠道 4 指标明细（UV/留资数/留资率/跳出率）；**漏斗留存率**：渠道到达→落地页；**意图数据**：SEO/广告来路搜索关键词 | 流量不精准/渠道匹配差 |
| ② | 落地页 | 落地的第一个具体页面（首页/详情页/营销页，不限首屏） | 跳出率 | **渠道/页面明细**：平均停留时长、页面跳出明细；**行为埋点·页面浏览**：滚动深度；**漏斗留存率**：落地页→站内浏览；**意图数据**：搜索关键词、站内搜索词 | 内容不匹配搜索意图/加载慢/首屏价值弱 |
| ③ | 站内浏览 | 落地后的浏览行为（可能落地页就走，也可能多页深看） | 无独立第一层信号（由「UV/跳出正常 + 留资跌」间接指向本段） | **渠道/页面明细**：访问深度（人均页数）、页面跳转；**行为埋点·页面浏览**：多页浏览路径/停留；**漏斗留存率**：站内浏览→转化入口触发；**意图数据**：站内搜索词 | 内容不吸引/站内导航差/没找到信息 |
| ④ | 转化入口触发 | 产生意向——点 CTA，**或主动找客服聊天** | 无独立第一层信号（由「前段正常 + 留资跌」间接指向本段） | **行为埋点·点击行为**：CTA 曝光/点击；**漏斗留存率**：站内浏览→转化入口触发；**意图数据**：客服对话意图（主动发起占比） | CTA 不明显/时机不对/没触发主动对话 |
| ⑤ | 转化交互 | **表单通道**（填了/没填就走）**或客服通道**（聊了就走/聊完留资），两条并行 | 无独立第一层信号（由「入口触发正常 + 留资跌」间接指向本段） | **行为埋点·交互行为**：表单填写进度、客服对话内容；**漏斗留存率**：转化入口触发→转化交互；**意图数据**：客服对话意图聚类与转化表现 | 表单太复杂/客服不专业/信任感不足/等待焦虑 |
| ⑥ | 成功留资 | 最终提交表单或通过客服完成留资 | 留资数、留资率；页面转化率排行（TOP5 最差/跌幅） | **行为埋点·转化行为**：留资字段内容、来源渠道；**漏斗留存率**：转化交互→成功留资；**渠道/页面明细**：页面转化率全排行 | 信任感不足/最后一步放弃/线索质量差 |

> **读表说明**：第一层信号 = 第 3 章「第一层 · 6 类必看指标」里与本段直接相关的子集；第二层下钻 = 第 3 章「第二层 · 按需调取」四类数据（渠道/页面明细、4 类行为埋点、漏斗留存率、用户意图数据）里本段会用到的子集——**两边同名同口径，不另造指标**。另：健康度（第一层第 5 类）是技术横切面，不属于任何漏斗段，但会干扰②落地页等段的归因，体检时必看。

> 为什么不是简单的"流量→转化"两段？因为 B2B 访客路径是长链条——可能落地页就走，也可能看多页再行动，行动还分"点 CTA 填表单"和"找客服聊"两条并行通道。漏斗切得越细，归因才能定得越准，措施才能调度得越具体。

### 1.2 全景图

```text
                    ┌─────────────────────────────────────────────────┐
                    │  定时触发（站点设置配置周期，≥7 天，默认 7 天）        │
                    └───────────────────────┬─────────────────────────┘
                                            ▼
  第 1 步 · 指标体检        读第一层 6 类指标（30 天日序列）→ 判定三类变化
  有没有事？                输出：变化清单（异常🔴 / 提升🟢 / 持平⚪）
                                            ▼
  第 2 步 · 漏斗段定位      每条变化对齐 6 段漏斗：渠道到达→落地页→站内浏览
  事在哪一段？              →转化入口触发→转化交互→成功留资
                          输出：变化段定位 + 初步归因方向
                                            ▼
  第 3 步 · 归因下钻        在定位段内下钻维度（渠道/页面/入口/通道/设备/地域/时段）
  是谁驱动/拖累的？          按需调第二层数据 + 4 类行为埋点
                          输出：具体到渠道×页面×入口的异常点
                                            ▼
  第 4 步 · 假设验证        生成假设（知识库命中 or 推理生成）→ 调数据验证
  为什么？（归因核心）       → 区分真因 vs 相关 → 落三套原因库
                          输出：根因 + 置信度 + 验证证据（可多根因并列）
                                            ▼
  第 5 步 · 应对方案        按变化类型分流：异常→措施清单（依据卡片）
  怎么办 / 怎么夸？          提升→夸赞文案；持平→说明+提升建议
                          输出：SolutionSet / PraiseResult / FlatResult
                                            ▼
  第 6 步 · 执行            仅异常类进本步：定三档边界（自动/确认后/只出方案）
  怎么落地？                → 调度 L4 五大产品模块执行 → 状态流转
                          输出：执行结果 + 执行前指标/销售快照
                                            ▼
  第 7 步 · 复盘            T+3/T+7/T+30 自动回看：执行前后对比 + 销售转化追踪
  效果怎样？                → 成功/部分改善/未改善 三档判定 → 沉淀知识库
                          输出：ReviewResult + Lesson（成功/失败都沉淀）
                                            │
                                            └──────► 知识库越用越厚 ──► 下一轮归因更准
```

### 1.3 七步速览表

| 步骤 | 回答的问题 | 核心输入 | 核心输出 | 对应产品方案模块 |
| --- | --- | --- | --- | --- |
| 1 · 指标体检 | 有没有事？ | 第一层 6 类指标（日序列） | 三类变化清单 | 模块 1 + 模块 2 |
| 2 · 漏斗段定位 | 事在哪一段？ | 变化清单 + 6 段留存率 | 变化段定位 | 模块 3 第一步 |
| 3 · 归因下钻 | 是谁拖累的？ | 第二层数据 + 4 类埋点 | 异常点（渠道×页面×入口） | 模块 3 第二步 |
| 4 · 假设验证 | 为什么？ | 假设 + 验证数据 | 根因 + 置信度 + 证据 | 模块 3 第三步 |
| 5 · 应对方案 | 怎么办/怎么夸？ | 根因（带变化类型） | 措施/夸赞/说明+建议 | 模块 4 |
| 6 · 执行 | 怎么落地？ | 异常类措施清单 | 执行结果 + 执行前快照 | 模块 5 |
| 7 · 复盘 | 效果怎样？ | 执行前后快照对比 | 判定 + 知识库沉淀 | 模块 6 |

---

## 2. 六条工作原则（方法论之魂）

这六条原则贯穿七步，是这套方法论区别于"规则报警器"的根本：

### 原则 1 · 数据先于判断 —— 先看数，再说话

Agent 看的不是"图"，是**结构化数值 + 30 天时间序列**。没有时间序列就只能靠死阈值判断，那是规则引擎，不是 AI。所有指标与客户看板**同口径、同数据源**，禁止二次加工产生歧义。

### 原则 2 · 三类变化一视同仁 —— 不只报忧，也报喜

| 变化类型 | 含义 | Agent 的应对 |
| --- | --- | --- |
| **异常（down）** | 跌破阈值/环比跌超阈值/连续下滑 | 归因 → 措施 → 执行 → 复盘（完整链路） |
| **提升（up）** | 突破正向阈值/环比涨超阈值/连续上升 | 归因 → 夸赞（不执行），让客户知道哪里做对了 |
| **持平（flat）** | 连续 N 期在正常区间内不动 | 归因 → 说明 + 提升建议（只出方案不执行） |

只报问题不报成绩，客户感受不到 AI 在持续干活。三类变化**归因深度一致**（都下钻到根因 + 置信度），只是产出和是否执行不同。

### 原则 3 · 假设必须可验证 —— 每个假设自带"验证清单"

Agent 生成的每个假设，必须附带"**如果是这个原因，应该还能观察到 XX 现象**"。然后逐条调数据验证：现象全成立 → 高置信度；部分成立 → 中置信度继续验证；全不成立 → 排除。不允许"我觉得可能是"这种不可验证的归因。

### 原则 4 · 诚实标注置信度 —— 做合理归因，不做因果断言

Agent 做的是"基于数据的合理归因"，不是严格的因果证明。所以**每个根因必须带置信度（高/中/低）**，多个根因允许并列、按置信度排序，不强行收敛成一个。宁可说"疑似外部因素，建议人工判断"，也不给"确定就是它"的假象。

### 原则 5 · 下钻有刹车 —— 该停就停，该喊人就喊人

无限下钻是归因最大的坑。四条停止条件（见第 12 章）：已到可执行粒度 / 数据不足 / 下钻超 3 层无果 / 置信度过低。喊人不是"我搞不定"，而是"已定位到 X 层，候选原因是 A/B，缺数据 C，建议人工介入"——**给信息，不甩锅**。

### 原则 6 · 成功失败都沉淀 —— 知识库不做偏科生

复盘后成功经验沉淀为"标准措施"（下次直接命中），失败经验沉淀为"负样本"（下次避坑），部分改善标注"调整后使用"。只沉淀成功的知识库是"只会开药不会避坑"的偏科生。这是这套方法论**越用越准**的根本机制。

---

## 3. 数据食谱：Agent 看什么、不看什么

### 3.1 三层数据视野

Agent 的数据视野严格分三层，**不多看也不少看**：

```text
第一层 · 异常探测层   → 每次分析必看，判断"有没有事"（6 类指标）
第二层 · 归因下钻层   → 发现变化后按需调取，定位"事在哪、为什么"
第三层 · 不进 Agent   → 给人看的图和展示，Agent 不分析
```

**第一层 · 6 类必看指标**（每次分析都拿，少一个就瞎一块）：

| # | 指标类 | 具体指标 | 回答什么 |
| --- | --- | --- | --- |
| 1 | 北极星 | 留资数、留资率 | 有没有事（最终业务结果） |
| 2 | 流量段 | UV、PV | 是流量问题还是转化问题（第一道分叉） |
| 3 | 承接段 | 跳出率 | 来了有没有接住 |
| 4 | 渠道维度 | 各渠道 UV、各渠道留资率 | 哪个渠道异常 |
| 5 | 健康度 | 健康度分 + 待处理问题列表 | 是不是网站本身有毛病（技术因素） |
| 6 | 页面排行 | 转化率最差 TOP5、跌幅 TOP5（UV≥10 过滤噪音） | 哪个页面在拖后腿 |

**第二层 · 按需调取**（平时不调，第一层发现问题后才调，全量塞进来噪音太大）：

第二层共四类数据，每类的具体内容、口径、调取时机如下——

**① 渠道/页面明细**（下钻"是哪个渠道/页面"时调）：

| 数据项 | 具体口径 | 什么时候调 |
| --- | --- | --- |
| 各渠道 4 指标明细 | 每个渠道（广告/社媒/SEO/直接/外链）各自的 UV、留资数、留资率、跳出率 | 北极星异常，需下钻到渠道 |
| 页面转化率全排行 | 全部页面的留资率排名（含最好和最差，UV≥10 过滤） | 渠道定位后，需下钻到具体页面 |
| 平均停留时长 | 按页面、按渠道拆分的停留时长 | 跳出率异常，判断"内容不行"还是"流量不精准" |
| 访问深度 | 人均浏览页数（人均 PV） | 同上，辅助判断承接质量 |
| 7 天/30 天日序列 | 第一层各指标的完整每日数值 | 判断是"突变"还是"趋势性下滑" |

**② 4 类行为埋点**（下钻"访客具体经历了什么"时调，按段取用）：

聚合指标只能告诉 Agent"哪段出事了"，**行为埋点告诉 Agent"访客在那段具体做了什么"**——这是归因到"为什么"的核心原料：

| 行为类 | 具体采集内容 | 归因到哪段时调 | 判断什么 |
| --- | --- | --- | --- |
| **页面浏览** | 浏览了哪些页面、每页停留时长、页面滚动深度 | ②落地页 / ③站内浏览 | 内容承接行不行：来了看不看、看多久、看多深 |
| **点击行为** | 点击了哪些按钮、链接、图片（含 CTA 的曝光与点击） | ④转化入口触发 | CTA 触达没触达：看到了吗、点了吗 |
| **交互行为** | 表单填写进度（填到第几个字段放弃）、鼠标悬停区域、客服对话内容 | ⑤转化交互 | 转化摩擦在哪里：卡在哪个字段、哪句对话后流失 |
| **转化行为** | 是否留资、留资字段内容、来源渠道 | ⑥成功留资 | 留资质量好不好：字段完整度、是否高价值线索 |

> 注意：4 类行为原始序列**只给 Agent 和运营侧看，不上客户看板**——看板只放聚合后的精简数（人均页数/平均停留/CTA 触达率/表单启动率），原始序列对客户是噪音。

**③ 6 段漏斗各段留存率**（定位"哪段漏最多"时调）：

| 段间 | 留存率口径 | 流失率口径 |
| --- | --- | --- |
| 渠道到达 → 落地页 | 进入网站且未立即跳出的会话占比 | 落地页跳出率 |
| 落地页 → 站内浏览 | 未跳出且浏览≥2 页的会话占比 | 落地页跳出率 |
| 站内浏览 → 转化入口触发 | 触发 CTA 或主动客服的会话占比 | 未触发转化入口率 |
| 转化入口触发 → 转化交互 | 进入表单填写或客服对话的会话占比 | 入口触发后未交互率 |
| 转化交互 → 成功留资 | 成功提交表单或客服留资的会话占比 | 转化交互流失率 |

**④ 用户意图数据**（归因"访客想要什么"时调）：

4 类行为埋点告诉 Agent"访客做了什么"，意图数据告诉 Agent"**访客想要什么**"——行为是结果，意图是动机。有了它，"内容不匹配"能从模糊判断升级为"哪个意图、错配到哪页"的精确结论：

| 意图数据源 | 具体口径 | 什么时候调 |
| --- | --- | --- |
| **搜索关键词** | SEO 来路搜索词（GSC/百度站长）+ 各词落地页；广告投放关键词 + 创意文案 | 归因落地页段，判断"搜索词意图 vs 落地页内容"是否错配 |
| **站内搜索词** | 站内搜索关键词、搜索次数、0 结果率、搜索后跳出率 | 归因落地页/站内浏览段，发现"内容缺口"（访客想要但网站没有的内容） |
| **客服对话意图** | 客服对话聚类出的意图标签（价格咨询/产品对比/售后/案例考察）+ 各意图占比与转化率 | 归因转化入口触发/转化交互段，分析"哪类意图的访客转化好/差" |

> 意图识别与聚类是 L3「搜索意图精准匹配」的活，归因 Agent 不重复建设——它是意图数据的**使用方**（拿意图做归因）+ **反哺方**（归因发现的新意图、意图转化表现回流 L3，让意图匹配越来越准）。

**第三层 · 不进 Agent 视野**：趋势图/饼图本身（图是给人看的）、上次的"转化洞察"文字（自己的产出不能当输入，会循环）、纯展示指标（如访问时段分布）、4 类行为原始序列不给客户看板（只给 Agent 和运营侧）。

### 3.2 第二层怎么用：按需调取，不一次全拉

第二层的取用逻辑是"**先判断，再调数**"，不是一次性全塞给 Agent：

```text
第一层发现异常
  → 判断异常类型（流量侧？承接侧？哪段？）
    → 决定调第二层的哪一类、哪个维度
      → 调回来再分析 → 不够再补调
```

**为什么必须这样**：全量塞进来噪音太大，Agent 会被无关数据带偏；按需调取让每一步分析都有明确的"我为什么调这份数"——这也写进了归因的分析过程里，客户能看到 Agent 的取数逻辑，可解释、可审计。

---

## 4. 第 1 步 · 指标体检：判断"有没有事"

> **一句话**：按配置周期定时醒来，把第一层 6 类指标扫一遍，判定哪些指标发生了哪类变化（异常/提升/持平），登记成变化清单。

**输入** → 站点 ID、分析周期（站点设置配置，≥7 天，默认 7 天）
**输出** → 变化清单（每条：指标、变化类型、变化值 vs 基准值、命中规则、级别），进智能体任务队列

### 4.1 动作序列

```text
定时器到点（周期可配 7/14/30 天）
  → 拉取第一层 6 类指标（30 天日序列）
  → 逐指标跑规则层判定（阈值/变化/趋势/持平四类规则，正负向都判）
  → 规则未命中但"感觉不对" → 模型兜底层介入（形态/联动/结构三类）
  → 汇总本期变化清单：异常🔴（分 P0/P1/P2）+ 提升🟢 + 持平⚪
  → 生成一份"指标变化报告"进智能体任务队列，状态 pending_attribution
```

### 4.2 判断逻辑

**变化判定三层架构**：

| 层 | 干什么 | 覆盖 |
| --- | --- | --- |
| 规则层（为主） | 阈值规则（绝对值破线）/ 变化规则（环同比超阈值）/ 趋势规则（连续 N 期同向）/ 持平规则（连续 N 期不动） | 80% 可解释变化 |
| 模型兜底层 | 形态异常（波动率突增/平台期破位）、联动异常（UV升+留资降+跳出升=流量变垃圾）、结构异常（渠道占比突变） | 20% 规则漏网的 |
| 沉淀闭环 | 模型命中的变化 → 人工标注 → 真异常沉淀成新规则 | 越用越准 |

**异常分级**（越靠近留资，级别越高）：

| 级别 | 触发条件 | 例子 |
| --- | --- | --- |
| P0 | 北极星异常（留资数/率触发任意规则） | 留资率环比跌 0.5pp |
| P1 | 过程指标异常（UV/跳出率/渠道） | 广告渠道 UV 跌 40% |
| P2 | 健康度/页面异常 | 死链暴增 23 条 |

提升/持平**不分级**，统一标记 `changeType: up / flat`。

### 4.3 阈值设计原则

- 所有阈值**可配置**，默认值是起点不是圣旨；
- **小流量站点放宽**：UV < 200 的站点阈值放宽 1.5 倍，避免天天误报；
- **提升判定从严**：正向阈值比负向略宽（如留资率涨 ≥0.5pp 才算提升），避免把噪音当成绩；
- **持平需连续 4 期**：单期不动不算持平，约一个月无变化才标记。

### 4.4 护栏

- **只定时触发**：不设手动/实时/事件触发——手动让客户养成自己找问题的习惯，与"AI 主动干活"冲突；T+1 日更下"实时"是伪实时；
- **模型判定必标注**：模型兜底产出的变化必须标"模型判定"，与规则命中区分，防止黑盒说不清"为啥报警"；
- **任务过期机制**：变化任务一个周期内未处理自动关闭，避免队列无限堆积。

---

## 5. 第 2 步 · 漏斗段定位：判断"事在哪一段"

> **一句话**：每条变化进来，先对齐 6 段漏斗，判断它发生在哪一段——这一步定下归因的大方向，跳步会导致归因停在表面。

**输入** → 变化清单（第 1 步产出）+ 第一层指标的相互关系
**输出** → 每条变化的变化段定位（6 段其一或全段）+ 初步归因方向

### 5.1 判断逻辑（以异常类为例的第一道分叉树）

```text
留资跌？
  ├─ + UV 跌                    → ① 渠道到达段（先治流量）
  ├─ + UV 没跌 + 跳出率升        → ② 落地页段（治落地页）
  ├─ + UV/跳出正常 + 平均页数跌  → ③ 站内浏览段（治导航/内容）
  ├─ + 前面都正常 + CTA 点击率低 → ④ 转化入口段（治 CTA/主动对话）
  ├─ + CTA 正常 + 表单/客服转化低 → ⑤ 转化交互段（治表单/客服）
  └─ + 留资量正常 + 字段质量差   → ⑥ 成功留资段（治信任/线索质量）
```

**提升类**：看"哪段变好了"——如留资涨 + SEO 渠道跳出率降 → ② 落地页段提升。
**持平类**：看"哪段都没动"——全段指标平稳 + 内容更新为 0 → 全段持平。

### 5.2 各段定位对应的归因方向

| 漏斗段 | 异常含义 | 初步归因方向 |
| --- | --- | --- |
| ① 渠道到达 | 来的人少了 | 流量侧（渠道、投放、SEO） |
| ② 落地页 | 来了没接住 | 落地页内容、加载、首屏价值 |
| ③ 站内浏览 | 接住了没深看 | 站内导航、内容吸引力 |
| ④ 转化入口触发 | 看了没行动 | CTA 时机/位置、主动对话策略 |
| ⑤ 转化交互 | 行动了没完成 | 表单复杂度、客服专业度、信任感 |
| ⑥ 成功留资 | 完成了质量差 | 信任感、最后一步放弃、线索质量 |

### 5.3 护栏

- **顺序执行**：定位 → 下钻 → 验证三步严格顺序，不允许跳步——"留资跌了"是废话，"广告渠道落地页承接变差导致留资跌"才是根因；
- 这一步的产出**只是方向**，不是根因，Agent 不得在此止步出方案。

---

## 6. 第 3 步 · 归因下钻：找到"是谁驱动/拖累的"

> **一句话**：在定位到的漏斗段内，按需调第二层数据（渠道/页面明细 + 4 类行为埋点），下钻到"具体是哪个渠道×哪个页面×哪个入口"出的问题。

**输入** → 变化段定位（第 2 步产出）
**输出** → 具体异常点（如"广告渠道落地页跳出率 52%，显著高于其他渠道 42%"）

### 6.1 动作序列：按段定下钻维度、按需调数

| 变化段 | 下钻维度 | 调取的数据 |
| --- | --- | --- |
| ① 渠道到达 | 渠道 | 各渠道 UV 明细 |
| ② 落地页 | 渠道 + 页面 + 搜索意图 | 渠道明细 + 页面跳出率/停留 + 页面浏览埋点（滚动深度）+ 搜索关键词/站内搜索词 |
| ③ 站内浏览 | 页面路径 + 页面 | 页面跳转、停留、访问深度 + 页面浏览埋点 |
| ④ 转化入口触发 | 入口类型(CTA/客服) + 页面 | 页面明细 + 点击行为埋点 |
| ⑤ 转化交互 | 通道类型(表单/对话) + 页面 | 交互行为 + 转化行为埋点 |
| ⑥ 成功留资 | 留资方式 + 线索质量 | 留资字段 + 线索质量评分 |

**下钻维度枚举固定**（渠道/页面/入口类型/通道类型/设备/地域/时段），避免 Agent 乱钻；优先用渠道 + 页面 + 入口/通道类型，设备/地域/时段作补充。

### 6.2 判断逻辑

- **按需调取，不一次全拉**：先看第一层判断异常类型 → 决定调第二层哪个 → 调了再分析。全量塞给 Agent 会噪音太大；
- **控制变量对比**：固定渠道看页面，固定页面看渠道。"只有广告渠道跳出率升、其他渠道没升"→ 不是全站落地页问题，是广告×落地页的匹配问题；
- **对照对比**：不止看绝对数——对比本站历史均值、其他渠道/页面均值，判断"是真异常还是波动噪音"。

### 6.3 护栏

- **UV 下限过滤**：页面排行必须过滤日 UV < 10 的页面，小样本转化率波动大，会把 Agent 带偏；
- **渠道枚举固定**：新增渠道必须显式通知 Agent，否则新渠道会被当异常处理；
- 这一步产出是"异常点"，**还不是根因**（为什么广告渠道跳出率高，要第 4 步验证）。

---

## 7. 第 4 步 · 假设验证：得出"根因 + 置信度"（归因核心）

> **一句话**：基于异常点生成可验证的假设，逐条调数据验证，区分真因 vs 相关，最终落到标准原因库——这是七步里最难、也最能体现"AI 含量"的一步。

**输入** → 具体异常点（第 3 步产出）+ 知识库（原因库 + 映射表）
**输出** → 根因（可多个并列）+ 置信度（高/中/低）+ 验证证据 + 原因分类（落三套原因库）

### 7.1 动作序列

```text
异常点进来
  → 生成候选假设（两个来源）：
      ① 知识库匹配：异常特征 → 命中标准原因（可解释、可复用）
      ② Agent 推理生成：知识库没命中 → 基于数据+行为埋点生成新假设
  → 每个假设附带"验证清单"：如果是这个原因，应该还能观察到 XX
  → 逐条调数据验证（渠道明细/页面明细/行为埋点/变更记录）
  → 判定置信度 → 区分真因 vs 相关 → 落原因库
  → 多根因并列输出，按置信度排序，不强行收敛
```

### 7.2 假设验证实例（案例 1 的真实过程）

```text
假设：广告渠道落地页与广告创意不匹配
  → 验证1：广告渠道落地页跳出率是否高于其他渠道？  52% vs 42% ✓
  → 验证2：广告渠道停留时长是否更短？              28s vs 全站 45s ✓
  → 验证3：广告渠道滚动深度是否更浅？              集中首屏，产品区到达率<30% ✓
  → 三条全成立 → 假设升级为"高置信度根因"
```

**置信度三档**：现象全成立 → 高；部分成立 → 中（继续验证或并列）；全不成立 → 排除。

### 7.3 区分真因 vs 相关（最难点）

| | 相关 | 真因 |
| --- | --- | --- |
| 定义 | A 和 B 同时变化 | A 导致 B |
| 判据 | 同时发生 | 时间先后 + 控制变量 |

Agent 的三个实操方法：

1. **时间先后**：流量跌在前还是留资跌在前？流量先跌 → 流量是因；同时跌 → 可能共因；
2. **控制变量**：固定渠道看页面、固定页面看渠道，隔离出独立影响；
3. **反证**：如果是这个原因，应该还有 XX 现象——查一下，没有就降置信度。

### 7.4 归因终点：三套标准原因库

Agent 的归因结论必须能落到对应类型的原因库之一（或标注"新原因，待审核沉淀"），防止乱归因：

| 原因库 | 对应 | 内容举例 |
| --- | --- | --- |
| **流失原因库** | 异常类·为什么跌 | ②内容不匹配搜索意图/加载慢/首屏价值弱；④CTA不明显/时机不对；⑤表单太复杂/客服不专业… |
| **提升原因库** | 提升类·为什么涨 | ②内容匹配搜索意图；①流量变精准；⑤表单顺/客服专业… |
| **持平原因库** | 持平类·为什么没动 | 内容无更新/流量结构稳定无新增/行业淡季/已到转化天花板/措施见效周期未到 |

### 7.5 护栏

- **下钻深度上限 3 层**（漏斗段 → 维度 → 假设验证），超层强制停，进喊人流程；
- **新假设要人工审核**：知识库没命中、Agent 自己生成的假设/原因，必须人工审核通过后才用、才沉淀，避免乱归因污染知识库；
- **喊人机制**：多假设无法站内验证 / 涉及外部因素（季节/竞对/政策）/ 下钻 3 层无果 / 同一异常反复且上次方案无效 → 输出"已定位到 X 层 + 候选原因 A/B + 缺数据 C"，挂起等人工，不硬猜。

---

## 8. 第 5 步 · 应对方案：判断"怎么办 / 怎么夸"

> **一句话**：根因带变化类型进来，按类型分流产出——异常出措施清单（每条带依据卡片），提升出夸赞文案，持平出说明 + 提升建议（只出方案）。

**输入** → 根因 + 置信度 + 变化类型（第 4 步产出）
**输出** → 异常类：SolutionSet（措施清单）；提升类：PraiseResult（夸赞）；持平类：FlatResult（说明+建议）

### 8.1 判断逻辑：按变化类型分流

| 变化类型 | 产出 | 去向 |
| --- | --- | --- |
| 异常（down） | 可执行措施清单（每条带依据卡片 + 建议边界 + 调度模块） | 进第 6 步执行 |
| 提升（up） | 夸赞文案（基于提升原因库，讲清哪里做对了）+ 保持建议 | 直接展示，不执行 |
| 持平（flat） | 说明文案（为什么没动）+ 提升建议（一律只出方案） | 直接展示，不执行 |

### 8.2 措施来源：双轨制

```text
根因进来
  → 先查知识库映射表（原因→措施）
    → 命中 → 直接用标准措施（可解释、可复用）
    → 没命中 → Agent 生成新措施
      → 人工审核（与"新假设要审核"同一套逻辑）
        → 通过 → 用，并沉淀回知识库
        → 不通过 → 退回重生成 or 喊人
```

### 8.3 措施结构：每条措施必须带"依据卡片"

每个措施是可被执行的完整单元，字段：措施描述、对应根因、根因置信度、**依据卡片**、措施类型（止血/治本）、成本、见效周期、风险、需调度的 L4 模块、建议执行边界。

**依据卡片三段式**（对齐老板"每个任务要有依据，但不露规则"的要求）：

```text
依据卡片 = 现状数据 + 对比基准 + 具体动作

例：广告渠道落地页跳出率 52%（现状）
    高于行业平均 42%（对比基准）
    建议按搜索意图重写落地页内容（具体动作）
```

缺对比基准，客户不知道"该不该急"；缺具体动作，客户不知道"做什么"。措施**不带预期效果字段**——复盘只看"执行前 vs 执行后实际变化"，不被预期绑架。

### 8.4 排序与搭配逻辑

- **先分组再排序**：止血组排前（P0 异常优先止血），治本组随后；组内按 根因置信度 → 影响面 → 成本 → 见效周期 定性排序，不搞加权公式（可解释、可调）；
- **P0 搭配规则**：至少 1 个止血 + 1 个治本；止血见效后治本仍要排上，别止完血就不管；
- **一根因可多措施**：如"落地页与创意不匹配"→ 措施 A 重写落地页 + 措施 B 调整广告创意指向，两个都给让客户选，不强行收敛。

### 8.5 护栏

- 模块 4 **只生成方案不执行**，也不拍板执行边界（那是第 6 步的事）——职责不分会糊在一起、无法独立评估；
- 提升/持平类产出**直接展示**，永远不进执行流程。

---

## 9. 第 6 步 · 执行：定边界 + 调度 L4 模块

> **一句话**：只接异常类措施，按"类型 + 风险 + 可逆性"定最终执行边界（自动/确认后/只出方案），调度 L4 已有产品模块执行，自己不动手改内容。

**输入** → 异常类措施清单（带建议边界 + 调度模块 + 依据卡片）
**输出** → 执行结果（成功/失败/拒绝/过期/只出方案）+ **执行前指标快照 + 销售快照**（供复盘）

### 9.1 三档执行边界

| 档 | 含义 | 客户动作 | 典型措施 |
| --- | --- | --- | --- |
| **自动执行** | Agent 直接调度，不等客户 | 无需操作，事后通知 | 404/死链修复、SSL 续签、sitemap 生成 |
| **确认后执行** | 方案备好，客户点确认才执行 | 必须点确认 | 落地页重写、CTA 优化、详情页优化 |
| **只出方案** | 只给建议，不调度 | 客户自行处理 | 涉及钱/外部：广告预算、外链、申诉 |

### 9.2 边界判定规则（关键：不看异常级别）

| 因素 | → 自动执行 | → 确认后执行 | → 只出方案 |
| --- | --- | --- | --- |
| 措施类型 | 技术止血类 | 内容优化类 | 涉及钱/外部 |
| 风险 | 低 | 低-中 | 中-高 |
| 可逆性 | 可逆（能回滚） | 部分可逆 | 不可逆 |
| 异常级别 | **不限（P0 也可自动）** | 不限 | 不限 |

**为什么 P0 允许自动执行**：技术止血类措施低风险、可逆、且止血刻不容缓——P0 死链暴增时等客户确认，反而耽误事。

### 9.3 调度机制：复用 L4 五大模块，不新建执行能力

| targetModule | 调度的 L4 模块 | 干什么 |
| --- | --- | --- |
| ai_content_engine | AI 内容引擎 | 按搜索意图重写/生成营销页、详情页 |
| conversion_path_designer | 转化路径设计器 | CTA 位置/时机、心理学触发器、A/B 测试变体 |
| smart_form | 智能表单系统（二期） | 字段精简、分步填写（一期只出方案） |
| ai_cs_pro | AI 智能客服 PRO | 补 FAQ/知识库、主动对话策略配置 |
| tool_agent | 工具/Agent 直调 | 404/SSL/sitemap/链接修复（技术止血） |
| none | 不调度 | 只出方案展示，客户自行处理 |

调度时把**依据卡片一并传给被调度模块**，作为执行依据留存。

### 9.4 执行流程与状态流转

```text
措施清单 → 定最终边界
  ├─ 自动执行   → 直接调度 → 成功/失败 → 通知客户
  ├─ 确认后执行 → 待确认队列（红点提醒）
  │    ├─ 客户确认 → 调度执行 → 成功/失败
  │    ├─ 客户拒绝 → 关闭（记原因）
  │    └─ 超时一周 → 自动关闭
  └─ 只出方案   → 展示（含依据卡片）→ 客户自行处理（不追踪）
```

状态机：`pending_confirm → executing → success / failed / rejected / expired / advice_only`。**失败不自动重试**——回退待确认让客户决定，失败原因进复盘。

### 9.5 护栏

- **执行前必留快照**：指标快照 + 销售转化快照。因为措施不带预期效果，复盘只能靠"执行前 vs 执行后"实际对比，没快照就没法复盘；
- **只出方案类不追踪**：客户自行处理，无法追踪就不追踪，不装模作样；
- **提升/持平永不进本步**——夸赞和建议没有"执行"一说。

---

## 10. 第 7 步 · 复盘：判定 + 沉淀，让飞轮转起来

> **一句话**：到点自动回看执行前后数据，判定措施成效（追到销售转化贡献），成功失败都沉淀回知识库——这是方法论闭环的最后一块，也是"越用越准"的引擎。

**输入** → 执行结果 + 执行前快照（第 6 步产出）+ 执行后当前数据
**输出** → ReviewResult（判定 + 变化幅度 + 后续动作）+ Lesson（沉淀知识库）

### 10.1 复盘周期：按措施见效周期分档，不一刀切

| 措施见效周期 | 复盘周期 | 适用 |
| --- | --- | --- |
| 即时/天 | T+3 | 止血类（404 修复、SSL 续签） |
| 周 | T+7 | 治本类（内容优化、CTA 优化） |
| 月 | T+30 | 长效类（结构化数据、外链） |

到复盘周期定时自动拉执行后数据对比，客户也可手动提前看。

### 10.2 两层判定标准

**第一层 · 业务指标判定**（执行前快照 vs 执行后实际值）：

| 判定 | 标准 | 后续动作 |
| --- | --- | --- |
| 成功 | 异常指标回归正常区间 | 关闭任务，沉淀标准措施 |
| 部分改善 | 有改善但未回归正常 | 继续观察或追加措施（回第 5 步） |
| 未改善/恶化 | 无变化或更差 | 重开归因（回第 4 步）或喊人 |

**第二层 · 销售转化贡献判定**（对齐"销售转化贡献追踪"，二期接 CRM 后完整启用）：

| 判定 | 含义 |
| --- | --- |
| 销售贡献显著 | 留资恢复，关联线索的销售转化率也提升 |
| 销售贡献待观察 | 留资恢复，销售转化周期长继续看 |
| 留资恢复但销售未跟进 | 留资量回来了但质量/跟进有问题 |

### 10.3 控制变量：多措施共效时怎么归因

| 场景 | 归因方式 |
| --- | --- |
| 单措施执行 | 直接对比，归因清晰 |
| 多措施同时执行 | 按各措施对应的指标维度分别看变化，尽量拆 |
| 多措施共效（难拆） | 标注"多措施共效"，不强行归到单个 |
| 外部因素干扰（季节/竞对/节假日） | 标注"疑似外部因素"，降置信度 |

**原则**：宁可标注"共效/外部因素"，不强行归因——假归因比不归因更有害，会误导知识库沉淀错误经验。

### 10.4 沉淀机制：三种结果都入库

| 复盘结果 | 沉淀为 | 作用 |
| --- | --- | --- |
| 成功 | 标准措施（场景+根因+措施+成功+销售贡献） | 下次同类直接命中 |
| 失败 | 负样本（场景+根因+措施+失败） | 下次同类避坑，不再推荐 |
| 部分改善 | "部分有效"标注，措施降级或调整后沉淀 | 下次同类调整使用 |

每条 Lesson 带齐：场景、6 段漏斗段、根因 + 原因分类、措施、调度的 L4 模块、复盘结果、变化幅度、销售贡献、下次建议（直接用/调整用/弃用）。

---

## 11. 知识库飞轮：为什么这套方法论越用越准

七步里有**四个沉淀点**，它们共用一个知识库，形成飞轮：

```text
┌────────────────────────────────────────────────────────────┐
│                      知识库（三套内容）                        │
│  ① 三套原因库（流失/提升/持平，按 6 段漏斗组织）               │
│  ② 根因→产出映射表（原因→措施/夸赞/建议）                     │
│  ③ 复盘经验库（标准措施 + 负样本 + 部分有效标注）               │
└────────────────────────────────────────────────────────────┘
        ▲                ▲                ▲
        │ 沉淀           │ 沉淀            │ 沉淀
  第1步·模型兜底      第4步·新假设       第7步·复盘结论
  命中→人工标注       →人工审核通过      （成功/失败/部分）
  →新规则            →新原因/新措施      →Lesson
```

**飞轮逻辑**：归因时先查库（命中→可解释可复用），查不到才推理（新假设/新措施→人工审核→沉淀）；执行后必复盘（成功/失败都沉淀）。于是知识库越用越厚，规则越用越多，模型兜底和人工审核的介入越来越少——**Agent 的经验在积累，而不是每次从零开始**。

---

## 12. 护栏总表：Agent 的分寸感从哪来

把散布在七步里的护栏集中列一张表，这是汇报时回答"AI 会不会乱来"的依据：

| 环节 | 护栏 | 防什么 |
| --- | --- | --- |
| 触发 | 只定时（≥7 天可配），无手动/实时/事件 | 防"客户自己找问题"的习惯回流 |
| 数据 | 三层视野，第三层（图表/展示）不进 Agent；口径与看板统一 | 防噪音、防二次加工歧义 |
| 判定 | 规则为主 + 模型兜底必标注 + 人工标注才沉淀 | 防黑盒报警说不清 |
| 下钻 | 维度枚举固定；UV<10 页面过滤；深度上限 3 层 | 防乱钻、防小样本带偏 |
| 归因 | 假设必须可验证；根因必带置信度；多根因并列不强行收敛 | 防"确定就是它"的假象 |
| 归因 | 落三套原因库，落不了标"新原因待审核" | 防乱归因 |
| 喊人 | 4 类场景挂起等人工，输出已定位层 + 候选 + 缺数据 | 防硬猜、给信息不甩锅 |
| 方案 | 新假设/新措施必须人工审核；措施不带预期效果 | 防未验证措施直接执行 |
| 执行 | 三档边界；涉钱/外部一律只出方案；失败不自动重试 | 防 Agent 碰钱碰外部 |
| 执行 | 执行前必留指标+销售快照 | 防复盘无据 |
| 复盘 | 多措施共效不强拆；外部因素降置信度 | 防假归因污染知识库 |
| 边界 | 不改 L4 模块、不改线 A 体检；自己不改内容/发产品 | 职责清晰、复用不重复造 |

---

## 13. 端到端走一遍：案例 1 的完整七步回放

用「落地页优化」案例把七步串起来，看 Agent 的一次完整工作：

| 步骤 | Agent 干了什么 | 产出 |
| --- | --- | --- |
| **1 · 体检** | 定时跑周期分析，发现留资率 3.2%→2.7%（环比跌 0.5pp，命中变化规则），UV 正常 | 异常🔴 P0：留资率下跌 |
| **2 · 定位** | UV 没跌 + 跳出率升 → 不是流量问题，定位 **②落地页段** | 变化段：落地页 |
| **3 · 下钻** | 调渠道明细：广告渠道跳出率 52%，其他渠道平均 42%；控制变量：仅广告渠道异常 | 异常点：广告渠道落地页 |
| **4 · 验证** | 知识库命中假设"落地页与创意不匹配"→ 验证：停留 28s<45s ✓、滚动浅 ✓ → **高置信度根因** | 根因：广告渠道落地页与创意不匹配（高） |
| **5 · 方案** | 映射表命中 → 措施"按搜索意图重写落地页内容"，依据卡片：跳出率 52% / 高于行业 42% / 重写落地页；治本类，调度 AI 内容引擎，建议确认后执行 | SolutionSet（1 条措施） |
| **6 · 执行** | 定边界：内容优化类 + 低风险 + 部分可逆 → **确认后执行**；客户点确认 → 调度 AI 内容引擎重写 → 成功；留执行前快照 | 执行成功 |
| **7 · 复盘** | T+7 自动回看：广告渠道跳出率 52%→44%，回归正常区间；其他渠道同期无变化（控制变量成立）→ **成功**；沉淀标准措施 | 任务关闭，Lesson 入库 |

**这条链路在客户视角的样子**：任务中心红点 → 点开归因报告 → 看到 Agent 的分析过程（定位→下钻→验证→结论）→ 看到带依据卡片的措施 → 点"确认执行" → 一周后收到复盘通知"措施起效，跳出率 52%→44%"。

**客户全程只做了一次点击（确认），其他都是 Agent 干的。**

---

## 14. 汇报一页纸

> 给老板汇报时的浓缩版，可直接念。

**这个 Agent 是什么**：一个会自己干活的增长运营专家。每 7 天（可配）自动把网站的业务指标体检一遍，发现变化就归因到根因，给出带依据的方案，经客户确认后调度现有产品模块执行，一周后自动复盘效果，经验全部沉淀——下次更准。

**三个关键词**：

1. **6 段漏斗** —— 所有归因的骨架：渠道到达→落地页→站内浏览→转化入口→转化交互→成功留资。指标挂在哪段，问题就定位到哪段，措施就调度到对应的模块；
2. **假设-验证** —— 归因的内核：每个假设自带验证清单，逐条调数据（含 4 类行为埋点）验证，区分真因和相关，结论必带置信度——不装确定、不乱归因；
3. **闭环沉淀** —— 越用越准的引擎：执行前后必对比（追到销售转化贡献），成功失败都沉淀知识库，下次同类直接命中。

**与其他产品的差异**：

| 传统数据看板 | 本 Agent |
| --- | --- |
| 告诉客户"指标跌了" | 告诉客户"为什么跌、怎么办、我帮你修、一周后告诉你修好没" |
| 只报忧 | 异常/提升/持平三类都分析，AI 干活的可见价值 |
| 报警靠规则 | 假设验证 + 置信度 + 喊人机制，可解释可审计 |
| 经验不积累 | 知识库飞轮，越用越准 |

**分寸感（老板最关心的一问）**：涉钱的一律只出方案（广告预算）、内容优化必须客户确认、只有低风险可逆的技术止血（如修死链）才自动执行、失败不自动重试、说不清的喊人——**AI 有明确的权力边界**。

---

## 附录 · 与产品方案 / 落地架构的映射

### A. 七步 ↔ 方案模块 ↔ skill/tool 映射

| 步骤 | 方案模块 | 落地形态（附录 A·方案 A） |
| --- | --- | --- |
| 1 · 指标体检 | 模块 1 + 模块 2 | skill 内推理 + T1-T4 数据 tool（第一层） |
| 2 · 漏斗段定位 | 模块 3 第一步 | skill 内推理 + T5 query_funnel_dropoff |
| 3 · 归因下钻 | 模块 3 第二步 | T6-T9（渠道/页面明细、行为埋点、意图数据） |
| 4 · 假设验证 | 模块 3 第三步 | skill 内推理 + T10 lookup_cause_action_map |
| 5 · 应对方案 | 模块 4 | skill 内推理（双轨制 + 依据卡片生成） |
| 6 · 执行 | 模块 5 | 调度 L4 模块（skill 间调度，非 tool） |
| 7 · 复盘 | 模块 6 | skill 内推理 + T11 save_lesson_to_kb |

**承载方式**：一期为单 skill `growth_diagnosis`（编排型，承载全链路）+ 11 个 tool（9 数据 + 2 知识库）；当 skill prompt 膨胀或归因质量上不去时，拆为编排层 + 4 个子 skill（方案 B）。

### B. 关键数据结构速查

| 结构 | 内容 | 流转 |
| --- | --- | --- |
| DiagnosisTask | 变化任务（变化类型/指标/规则/级别/快照） | 第 1 步 → 任务队列 |
| AttributionResult | 归因结果（漏斗段/根因+置信度/证据/下钻深度） | 第 4 步 → 第 5 步 |
| SolutionSet / PraiseResult / FlatResult | 三类应对产出 | 第 5 步 → 第 6 步或直接展示 |
| ExecutionResult | 执行结果 + 执行前指标/销售快照 | 第 6 步 → 第 7 步 |
| ReviewResult + Lesson | 复盘判定 + 知识库沉淀 | 第 7 步 → 知识库 |

---

## 修订记录

| 版本 | 日期 | 说明 |
| --- | --- | --- |
| v1.0 | 2026-07-30 | 首版：七步工作流方法论，与产品方案 v1.1 模块 1-6 严格对齐 |
| v1.1 | 2026-07-30 | 6 段漏斗提前至 1.1（分析骨架）；第 3 章第二层数据写明细；意图数据对齐产品方案 v1.2 |
| v1.2 | 2026-07-30 | 去掉客户分层与行业基准（第二层改为四类）；漏斗段间留存率改用全称（渠道到达→落地页…）；意图数据顺延为④ |
| v1.3 | 2026-07-30 | 1.1 骨架表「关键数据」拆为「第一层信号 / 第二层下钻」两列，第二层与第 3 章数据食谱四类数据同名同口径对齐 |
